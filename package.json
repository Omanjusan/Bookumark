{
  "name": "bookumark",
  "version": "0.1.0",
  "private": true,
  "description": "under construction - customizable bookmark firefox add-on",
  "scripts": {
    "build": "tsc",
    "experiment": "web-ext run --source-dir . --firefox=flatpak:org.mozilla.firefox --firefox-profile=develop --keep-profile-changes",
    "lint:ext": "web-ext lint --source-dir ."
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "typescript": "^7.0.2",
    "web-ext": "^8.0.0"
  }
}
