# 進捗

更新日: 2026-07-25

## 完了した作業

### フォルダ階層・移動モード（P9-1〜P9-8）

- Firefoxブックマークのツリーデータ契約を追加した。
- 現在フォルダの保存、復元、削除時の親またはルートへのフォールバックを実装した。
- ルートから現在フォルダまでのパンくず配列生成を実装した。
- フォルダ領域の表示と、フォルダクリックによる階層移動を実装した。
- カスタム表示順をフォルダ単位で保存・復元する形式へ移行した。
- フォルダとブックマークの表示母集団を分離した。
- 移動操作を「カスタム配置」「通常」「公式整理」の排他的な3状態に変更した。
- カスタム配置による同一フォルダ内の並べ替えを実装した。
- Firefox公式順の同階層D&Dを実装した。
- ブックマークとフォルダの公式階層移動を実装した。
- 循環、特殊ルート、検索中、API失敗に対する安全制御と表示復旧を実装した。
- 公式移動の結果通知とUndoを実装した。
- `<`と`>`によるフォルダ移動履歴の前後移動を実装した。
- 保存フォルダへの復帰時に、祖先経路をナビゲーション履歴へ復元するよう修正した。
- Firefox実機で階層移動、モード分離、復元、Undoを確認した（ユーザー確認済み）。

### ドッキングベイ

- ドッキングベイの仕様を `docs/dockingbay.md` に策定した。
- チップ、固定ベイ、ベイ工場、名前付きレイアウト、4レール配置、保存・復旧の実装フェーズをDB-1〜DB-15として定義した。
- DB-1「チップ種別と状態契約」を実装した。
  - 条件、操作、アクションの3種類を判別共用体として分離した。
  - チップ種別の分類をインスタンスではなく定義側で保持するようにした。
  - 条件チップの順次適用、後勝ち、異なる状態キーの合成を実装した。
  - 操作チップが共通状態を読み書きする契約を追加した。
  - アクションチップが明示実行時だけ動作する契約を追加した。
  - 文字ベースのテスト用チップで正常系、異常系、境界値を確認した。
  - 全48テストファイルの成功を確認した。
- DB-2「固定ベイ」を実装した。
  - 上レールへ検索ベイを固定配置した。
  - 下レールへフォルダ履歴、ソート、移動モードの3ベイを固定配置した。
  - フォルダ領域と公式移動通知はベイ外に維持した。
  - ベイ名をアクセシブルネームとして付与し、既存コントロールのIDと操作契約を維持した。
  - 狭いパネルでは下レールのベイが折り返すようにした。
  - 全49テストファイルの成功を確認した。
- DB-3A「既存表示機能の配置非依存化」を実装した。
  - 検索語、フィルタ、ソート、移動モードをまとめる共通状態コントローラーを追加した。
  - パネル本体をDOMやベイ配置に依存しない状態更新へ接続した。
  - 検索またはフィルタ開始時に移動モードを通常へ戻す連携を固定した。
  - 文字ベースのテスト用フィルタで検索、フィルタ、ソートの合成を確認した。
  - 入力データ、フィルタ、更新前状態を変更しないことを確認した。
  - 全50テストファイルの成功を確認した。
- DB-3B「訪問状態フィルタUI」を実装した。
  - 検索ベイ直後へ訪問状態フィルタベイを固定配置した。
  - 「すべて」「訪問あり」「未訪問」の排他的な3択を追加した。
  - 「訪問あり」は訪問回数が1以上、「未訪問」は訪問回数が0の項目だけを対象とした。
  - 訪問回数が欠損した項目は「訪問あり」「未訪問」のどちらにも混在させないようにした。
  - フィルタ開始時に移動モードを通常へ戻し、表示集合を即時再計算するようにした。
  - 全52テストファイルの成功を確認した。
- DB-3C「表示形式と切替UIの仕様」を策定した。
  - 初期版の表示形式を「パネル」「アイコン」「カード」「一覧」の4形式とした。
  - 未設計でカードと役割が重なる「詳細」は初期版から保留した。
  - 共通の`activeViewType`、切替UI、情報量、共通操作、状態遷移を定義した。
  - DB-3Dを状態と切替UI、一覧、アイコン、カード、連携回帰の5フェーズへ分割した。
- DB-3D-1「表示形式状態と切替UI」を実装した。
  - 共通状態へ`activeViewType`を追加し、初期値を`panel`とした。
  - 下レールのソートベイと移動モードベイの間へ表示形式ベイを配置した。
  - 「パネル」「アイコン」「カード」「一覧」の排他的な4択を追加した。
  - 表示形式を変更しても検索、フィルタ、ソート、移動モードを維持するようにした。
  - 複数の表示形式UIを同じ共有状態へ同期できる入力接続を追加した。
  - 選択値をメイン領域の`data-view-type`へ反映した。
  - 非パネル形式の描画はDB-3D-2〜DB-3D-4へ保留した。
  - 全54テストファイルの成功を確認した。
- DB-3D-2「一覧式」を実装した。
  - 共通表示集合の順序を維持したまま、アイコン、タイトル、ドメインを1行表示するようにした。
  - 一覧行へ既存の`.panel-tile`、`data-guid`、`data-url`契約を適用した。
  - クリック、カスタム配置、公式整理、先頭末尾drop境界を既存処理と共有した。
  - 440px以下ではドメインを隠し、タイトルを優先するようにした。
  - 不正URLのドメインを空表示にし、入力データを変更しないようにした。
  - 旧一覧に存在した削除ボタンは復活させなかった。
  - 全56テストファイルの成功を確認した。
- DB-3D-3「アイコン式」を実装した。
  - 共通表示集合の順序を維持した均一アイコングリッドを追加した。
  - 52pxの共通ブックマークアイコンと最大2行のタイトルを表示するようにした。
  - 86px以上の自動列、列間10px、行間20pxを基準にした。
  - 訪問回数、最終訪問日時、ドメインを常時表示しないようにした。
  - クリック、カスタム配置、公式整理、先頭末尾drop境界を既存処理と共有した。
  - アイコンタイルは縦長でも左右半分でdrop前後を判定するようにした。
  - 具体寸法の最終調整はFirefox実機確認へ保留した。
  - 全58テストファイルの成功を確認した。
- DB-3D-4「カード式」を実装した。
  - 48pxのアイコン、タイトル、ドメインを均一カードへ表示するようにした。
  - 訪問回数と最終訪問日時を省略しない独立した2行へ表示するようにした。
  - 訪問回数を実数値、未訪問、履歴不明の3状態で区別した。
  - 最終訪問日時をローカル時間の`YYYY/MM/DD HH:mm`で表示した。
  - 最終訪問日時の欠損・不正値を「記録なし」とした。
  - 省略表示時も各メタ行の全文を`title`へ保持した。
  - 最小幅270px、最小高さ96pxとし、内容に応じて高さを拡張できるようにした。
  - クリック、カスタム配置、公式整理、先頭末尾drop境界を既存処理と共有した。
  - 全60テストファイルの成功を確認した。
- DB-3D-5「4形式の連携回帰」を完了した。
  - 4形式の分岐をDOM非依存の表示形式プレゼンターへ抽出した。
  - パネル、アイコン、カード、一覧が同じ検索・フィルタ・ソート結果を使うことを固定した。
  - 選択中の表示形式だけを1回描画するようにした。
  - 0件時は全形式で共通の空状態を表示するようにした。
  - パネル式だけがグリッド寸法を待ち、他の3形式は寸法未確定でも描画するようにした。
  - カスタム配置、公式整理、通常、検索中、フィルタ中、公式移動処理中のD&D判定を共通化した。
  - 入力項目と共有状態を変更しないことを確認した。
  - 全62テストファイルの成功を確認した。
- DB-3「固定状態の機能テスト」全体を完了した。
- DB-4A「永続モデルとID発行」を実装した。
  - ベイ設定、メインレイアウト、ドッキングメタデータを3つのversion付き文書へ分離した。
  - ベイ、チップインスタンス、レイアウトの変更不能IDとデータ契約を追加した。
  - `kind`を保存せず、`chipType`に対応する定義側で保持するDB-1契約へ統一した。
  - ベイ、チップ、レイアウトへ独立した単調増加カウンターを追加した。
  - 削除済みIDを再利用せず、安全整数の上限を越えないID発行を実装した。
  - 具体的な内部デフォルト内容を注入値として扱う空文書生成を追加した。
  - 全63テストファイルの成功を確認した。

## 次に着手する作業

### DB-4B: ベイ・チップの構造正常化

保存されたベイとチップについて、schema version、ID、名前、`permanent`、設定JSON、重複ID、順序、採番カウンターを検証する。正常部分を維持し、不正部分だけを除外または補正して連続orderへ再採番する。

## 保留・TODO

- エラー、警告、成功通知を共通の入口で扱う統合通知機能を設計する。詳細は `docs/todo.md` を参照する。
