# 進捗

更新日: 2026-07-31

## ファイルの位置づけ

- `progress.md`: DB-14以降で更新する進捗の正本。
- `docs/progress-through-db13.md`: DB-13までの履歴を保持する読み取り用アーカイブ。
- `docs/progress.md`: DB-1着手前に作成された旧資料。履歴保持のため残すが、進捗の正本として扱わない。

## 完了済み

- DB-13「適用順とメイン再構築」まで完了済み。
- DB-13A〜DB-13Jの実装、回帰、完了監査を完了し、ユーザー完了確認済み。

## 最終検証結果

- 全137テストファイル成功。
- Firefox拡張lint: errors 0 / warnings 0 / notices 0。
- `git diff --check`: 問題なし。

## DB-14への引き渡し事項

- unknown/deprecatedチップの最終復旧UX。
- condition失敗の最終通知UI。
- 具体的な追加condition/action型。
- キーボードによる配置・並べ替えは対象外。

## 保留・TODO

- エラー、警告、成功通知を共通の入口で扱う統合通知機能はDB-14で設計済み、実装待ち。元要求は `docs/todo.md`、確定計画は `docs/db-14plan.md` を参照する。

## 再開位置

### DB-14: 廃止・破損復旧

DB-14の要求分析、懸念事項のインタビュー、仕様確定、テストケース設計、TDD実装フェーズ分割まで完了した。共通ダイアログと共通トーストをDB-14で作り、unknown/deprecated復旧とcondition失敗通知へ接続する。

- DB-14A Red「共通通知モデルとキュー」のテストを追加した。
  - ダイアログの単一active、FIFO、処理中ガードをテスト化した。
  - トーストの集約、発生回数更新、警告の8秒再計時、エラーの手動終了をテスト化した。
  - 不正契約の拒否、防御的スナップショット、存在しない通知への無変化応答をテスト化した。
  - プロダクションビルド成功後、未実装の`common-notification-queue.js`が存在しないことを理由に対象テストが失敗し、Red成立を確認した。
- DB-14A Green「共通通知モデルとキュー」を実装した。
  - ダイアログを単一activeとFIFO待機列で管理し、処理中の完了・重複操作を停止するようにした。
  - トーストを集約キーで更新し、発生回数と最新内容を維持するようにした。
  - warningを8秒後に自動終了し、同一通知の更新時に再計時するようにした。
  - errorを自動終了せず、手動終了できるようにした。
  - 通知契約を検証し、入力と返却スナップショットを防御的に分離した。
  - 全138テストファイルの成功を確認した。
- DB-14B「共通ダイアログ・トーストUI」をRed-Greenで実装した。
  - 見出しと説明を関連付けたネイティブ共通ダイアログを追加した。
  - politeなlive regionを持つ共通トースト領域を追加した。
  - active通知の内容、重要度、処理中状態、主操作をダイアログへ同期するようにした。
  - Escapeによる復旧フローの迂回を防ぎ、開始前要素へのフォーカス復帰を追加した。
  - トーストの順序、重要度、発生回数、手動終了を描画するようにした。
  - 狭いパネルと長文でも操作を維持するダイアログ・トーストCSSを追加した。
  - 入力スナップショットを変更せず、再描画時に古いトーストDOMを残さないことを確認した。
  - 全140テストファイルの成功を確認した。
- DB-14C「現行台帳・廃止台帳・分類」をRed-Greenで実装した。
  - 基本6 controlの型、表示名、kindを現行チップ台帳へ集約した。
  - rendererの型一覧とベイ工場の表示名解決を現行台帳の正本へ接続した。
  - プロダクション廃止台帳を空で開始し、テストでは複数世代の廃止レコードを注入できるようにした。
  - 現行、廃止、未知をバージョン比較に依存せず分類するようにした。
  - 全ベイのチップをベイID、ベイ名、instance ID付きで保存順に分類するようにした。
  - 現行・廃止の重複、両台帳の重なり、不正レコードを台帳生成時に拒否するようにした。
  - 分類結果と台帳索引を入力から防御的に分離した。
  - 全141テストファイルの成功を確認した。
- DB-14D「unknown部分復旧候補」をRed-Greenで実装した。
  - 現行、廃止、unknownが混在する文書からunknownだけを除外するようにした。
  - 残るチップの相対順を維持し、各ベイ内のorderを1始まりで再採番するようにした。
  - 全チップ除外後も空ベイ、レイアウト配置、メタデータを維持するようにした。
  - ベイ・チップ採番カウンターと正常チップのID・settingsを維持するようにした。
  - 除外対象をベイID、ベイ名、instance ID、chipType付きで保存順に返すようにした。
  - unknownがない場合は保存不要の無変更結果を返し、復旧済み候補への再適用を冪等にした。
  - 入力文書と返却候補・除外情報を防御的に分離した。
  - 全142テストファイルの成功を確認した。
- DB-14E「unknown／構造復旧保存セッション」をRed-Greenで実装した。
  - 既存の構造正常化結果とunknown除外候補を1つの保存候補へ統合した。
  - 変更文書をベイ、レイアウト、メタデータの固定順で和集合化し、1回のstorage要求で保存するようにした。
  - 復旧保存前・保存中・保存失敗時はruntime開始ゲートを閉じるようにした。
  - 復旧不要時はstorage要求を発生させず、初期状態からruntime開始可能にした。
  - 保存失敗後も候補と復旧情報を維持し、防御的コピーから同一候補を明示再試行するようにした。
  - 保存中の多重要求を拒否し、成功後の再実行では追加保存しないようにした。
  - 文書別recovery、変更文書、除外unknownを防御的スナップショットで返すようにした。
  - 全143テストファイルの成功を確認した。
- DB-14F「deprecated確認・削除セッション」をRed-Greenで実装した。
  - deprecatedインスタンスを型別・ベイ別に集約し、表示名、件数、廃止時期を通知用に返すようにした。
  - replacementChipTypeを現行台帳の表示名付き代替候補へ解決するようにした。
  - 確認前・保存中・保存失敗時はruntime開始ゲートを閉じるようにした。
  - 確認時に全ベイから全deprecatedを削除し、残るチップを連続orderへ再採番するようにした。
  - 空ベイ、配置、メタデータ、採番カウンター、正常チップ設定を維持するようにした。
  - 保存失敗後も集約情報と同一削除候補を維持し、防御的コピーから再試行するようにした。
  - 削除保存成功を確認済みの正本とし、保存済み文書の再検査で通知を繰り返さないようにした。
  - 全144テストファイルの成功を確認した。

- DB-14G「condition失敗通知アダプター」を完了した。
- condition適用失敗を1件のwarningトーストへ集約し、ベイ名・表示名・件数だけを利用者向け文面へ表示するアダプターを追加した。
- instanceId、chipType、reason、bay情報を診断情報として保持し、内部IDや失敗理由を通知本文へ露出しないことを確認した。
- 台帳や適用情報が欠ける場合は安全な表示へフォールバックし、入力を変更しないことを確認した。
- 全145テストファイルの成功を確認した。

- DB-14H「パネル起動・runtime統合」を完了した。
- 初回ロードの正常化を自動保存から分離し、unknown／構造復旧、deprecated確認・削除、通常runtime開始を固定順序で調停する起動オーケストレーターを追加した。
- 保存成功までruntime開始ゲートを閉じ、失敗時は共通ダイアログから同一候補を再試行できるようにした。
- 復旧不要時はダイアログと保存要求を増やさず、最終保存済み文書からruntimeを1回だけ開始することを確認した。
- 共通通知キュー／ビューをパネルへ接続し、condition失敗を診断ログと集約トーストへ通知した。警告トーストの自動終了も画面へ同期する。
- 全146テストファイルの成功を確認した。

- DB-14I「全体回帰・Firefox拡張lint」を完了した。
- 全146テストファイルの成功を確認した。
- Firefox拡張lintはerrors 0／warnings 0／notices 0を確認した。
- `git diff --check`に問題がないことを確認した。
- `web-ext`の更新確認はローカル設定ストアの権限案内を出したが、lint検証結果には影響していない。

- DB-14J「完了条件確認とDB-15引き渡し」の監査を実施した。
- DB-14A〜Iの機能、全146テスト、Firefox拡張lint、差分検査は成功済みである。
- 最終パネル表示には、unknownのベイ名・型・件数、構造復旧内容、deprecatedのベイ名・廃止時期・代替候補、保存失敗後の再試行表示が反映されていないことを確認した。
- 完了条件を満たさないためDB-14は完了扱いにせず、DB-15への正式引き渡しも保留する。
- 是正フェーズDB-14K「復旧ダイアログ最終表示」（コストM）を追加した。詳細は `docs/db-14plan.md` のDB-14J完了監査を参照する。

- DB-14K「復旧ダイアログ最終表示」をRed-Greenで完了した。
- unknownをベイ名・`chipType`・件数単位で集約し、構造補正と内部デフォルト復元を利用者向け名称で表示するようにした。
- deprecatedのベイ別件数、廃止時期、任意の代替候補を表示するようにした。
- 保存失敗後もactiveダイアログとキュー順を維持し、説明と主操作を「保存を再試行」へ更新するようにした。
- instance IDや例外詳細を画面へ出さず、入力スナップショットを変更しないことを確認した。
- 全147テストファイルの成功、Firefox拡張lintのerrors 0／warnings 0／notices 0、`git diff --check`の成功を確認した。
- DB-14Jで検出した未達5項目はすべて是正済みで、DB-14の技術的完了条件を満たした。

- 2026-07-29にユーザー最終確認を受け、DB-14「廃止・破損復旧」を完了した。

## DB-15への引き渡し

- Firefox実機での長文通知、ズーム、フォーカス、性能確認。
- 具体的な追加condition/action型。
- 既存のロード失敗・保存失敗などを共通通知へ全面移行する作業。
- 通知履歴。
- deprecatedチップから代替チップへの設定変換・自動置換。

## DB-15「Firefox実機・性能確認」計画

- 要求分析と全8問の懸念事項インタビューを完了した。
- DB-15をFirefox実機・性能確認と、発見した必須問題の必要最小限の是正に限定した。
- 正式対象環境をLinux版Firefox Flatpak、`develop`プロファイルに確定した。
- 高負荷fixtureを2,000ブックマーク、20ベイ、各10チップ、4レール使用に確定した。
- 5ズーム倍率、3画面条件、4系統のD&D、長文通知、フォーカス、性能測定の対象と合格方針を確定した。
- 追加condition/action型、既存通知経路の全面移行、通知履歴、deprecated自動置換はDB-16以降へ分離した。
- DB-15A〜DB-15Jの機能別フェーズとコストを`docs/db-15plan.md`へ記録した。
- DB-15の実装・実機検証には未着手。

次はDB-15A「実機fixtureと記録様式」のテストケース確認とGO確認から再開する。

- DB-15A「実機fixtureと記録様式」の自動化部分をRed-Greenで実装した。
  - 20ベイ、各10チップ、合計200チップを4レールへ決定的に配置するfixture生成を追加した。
  - productionの正常化を無変更で通過し、呼び出し間で状態を共有しないことをテストした。
  - 開発ビルド専用fixtureページを追加し、投入前のドッキング設定退避、2,000ブックマーク生成、fixture専用フォルダ削除、設定復元の導線を用意した。
  - production buildからfixtureページと実行コードを除外した。
  - `docs/db-15-results.md`へ実機結果と性能測定の記録様式を追加した。

次はDB-15AのFirefox実機投入・復元確認から続行する。

- DB-15A実機確認で、2,000件を作成したfixtureフォルダがBookumarkの現在フォルダへ選択されず、表示上の所在を判断できない必須問題を検出した。
- DB15-A-01として、`currentFolder`を退避・復元対象へ追加し、投入完了時にfixtureフォルダを現在フォルダへ設定するRed-Green是正を行った。
- DB-15Dの調査用に、4レールへ識別可能なベイを1つずつ配置するベイ編集調査fixtureと、高負荷fixtureへ戻す切替導線を追加した。
- DB-15D実機確認で、ベイ工場の対象選択アウトラインが表示されないDB15-D-01を検出した。
- ベイ工場の選択ID通知を4レールの実DOMへ接続し、対象ベイだけを3pxアウトラインで表示するRed-Green是正を行った。
- DB-15D実機画像からチップツール欄が空であることを確認し、空台帳と空D&D callbackが残るDB15-D-02を検出した。
- 現行6チップ台帳をツールセレクターへ接続し、追加・並べ替え・領域外削除をベイ編集セッションへ反映するようにした。
- ベイ名、Undo・Redo、保存状態を編集セッションへ接続し、保存後にレイアウト管理状態と通常runtimeを同じ文書から再構築するようにした。
- DB-15Dのベイ選択アウトライン、チップツール、追加、並べ替え、Undo・Redo、保存、再読込後の維持についてFirefox実機合格を確認した。
- DB-15D実機確認で、ベイ内部のチップ方向をレール内の複数ベイ配置へ誤用していたDB15-D-03を検出した。
- 上下レールではベイを垂直、左右レールでは水平に配置し、上・左・右・下の全レールで保存順が外側から内側へ増えるよう、逆方向レールのD&D座標と挿入マーカーを含めてRed-Green是正した。
- DB-15D実機確認で、新規ベイが編集トランザクションへ接続されずチップを置けないDB15-D-04を検出した。
- 新規ベイを一時ID付きの通常編集セッションへ接続し、チップ追加・並べ替え・Undo・Redo後の保存で正式ベイIDへ置換するようRed-Green是正した。
- DB-15D実機確認で、新規保存後の名前変更が直前ベイの改名になり、配置先も上固定であるDB15-D-05を検出した。
- 新規作成は「保存して閉じる」で成功時に終了し、次回追加では別IDを発行するようにした。4方向の配置先選択、各方向の内側末尾配置、同名拒否時のドラフト保持をRed-Greenで追加した。
- DB15-D-03〜D-05のレール配置、新規ベイ編集、保存終了、4方向配置、別名追加、同名拒否についてFirefox実機合格を確認した。
- DB-15E端パン確認で、レールにスクロール軸は設定されているが表示領域の上限がなく、内容に合わせて拡大するためスクロールバーと端パン条件が成立しないDB15-E-01を確認した。
- 端パンの目的は、レールからあふれた見えない挿入位置へD&D中に到達させることである。レール上限寸法と画面占有率の仕様が未確定のため、現状維持のうえペンディングとし、後続で仕様策定・実装・実機確認する。
- DB-15Eは端パン以外のブックマークD&D確認へ継続する。
- DB-15Eのカスタム配置D&Dについて、挿入位置、再読込後の独自順序維持、Firefox本体の順序非変更、キャンセル時の表示解除をFirefox実機で確認した。
- DB-15Eの公式整理・同階層D&Dについて、Firefox本体への反映、成功通知、Undo、再読込後の維持、処理中ガードをFirefox実機で確認した。
- DB-15Eの公式整理・別フォルダD&Dについて、fixture内フォルダへの移動、Firefox本体との一致、Undoによる復帰をFirefox実機で確認した。
- DB-15Fの長文通知・フォーカス検証用に、20ベイ・200種類の未知チップを安全な復旧対象として投入する開発fixtureを追加した。復旧後は高負荷fixtureへ戻せる。
- 20空ベイが200%・狭幅で通常UIを操作不能にすることをDB15-E-01の波及証跡として記録した。DB-15Fの検証責務を分離するため、長文量は未知チップ200種類のまま上レール1ベイへ集約し、復旧後に残る空ベイを1個へ限定した。
- DB-15Fの長文復旧ダイアログについて、200%・狭幅で主操作フォーカス、本文スクロール、フォーカス閉じ込め、内部詳細の非表示、復旧後操作をFirefox実機で確認した。
- トーストのフォーカス非奪取は自動DOMテストで維持されている。実機発火に必要な具体的condition型はDB-15対象外のため、実機確認を後続へ引き渡した。
- DB-15Gの性能初回測定へ移行した。
- Node補助ベンチマークで2,000件の描画計画を100回測定し、中央値0.913ms、p95 1.376ms、最大1.790msを記録した。
- Firefox実機の7操作を同じ区間で5回ずつ測るため、開発ビルドだけに二重`requestAnimationFrame`基準の計測パネルを追加した。試行値は再読込をまたいで保持し、JSONコピーできる。
- Firefox性能初回測定の途中値を取得した。起動5回は中央値355ms・最大362ms、表示形式切替9回は中央値107ms・最大147ms、検索2回は中央値104ms・最大105ms、レイアウト切替7回は中央値105ms・最大111ms、D&D再描画7回は中央値27ms・最大88msだった。
- 検索をあと3回、ソートとレイアウト編集開始を各5回追加測定してから合格基準案を確定する。
- Firefox性能初回測定の全7項目を取得した。起動は中央値355ms・最大362ms、対話操作は中央値最大107ms・単発最大147msで、操作不能、著しい待ち時間、未処理停止はなかった。
- 合格基準案を、起動は中央値500ms以下かつ最大750ms以下、6つの対話操作は各中央値150ms以下かつ最大250ms以下とした。ユーザー合意後にDB-15Hの確定基準とする。
- ユーザー合意によりDB-15Hの性能基準を確定した。起動は中央値500ms以下・最大750ms以下、対話操作6項目は各中央値150ms以下・最大250ms以下とし、操作不能・データ欠落・未処理停止は時間にかかわらず不合格とする。
- DB-15Hの最終性能測定を完了した。起動は中央値350ms・最大409ms、対話操作は中央値最大105ms・単発最大144msで全基準を満たした。
- 最終JSONで未採取だったレイアウト切替は、対象実装変更を挟んでいない直前の同条件測定（中央値105ms・最大111ms）を採用した。
- 性能測定中に操作不能、データ欠落、未処理停止は確認されず、DB-15Hの性能条件を合格とした。
- DB-15Iの全条件再検証・回帰へ移行する。
- DB-15Iの表示再検証で、ベイ編集調査fixtureと2,000ブックマークを用い、5ズーム倍率と3画面条件の全15組み合わせをFirefox実機で確認した。
- 全条件でヘッダー到達、ブックマーク表示、ページ全体の横スクロール非発生、主要チップ操作、文字・ボタンの非崩壊を確認した。DB15-E-01の20ベイ無制限展開は既知のPENDINGとして判定から除外した。
- DB-15Iの全体回帰で全153テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-15Aのfixture復元実機確認後にDB-15J完了監査へ進む。
