# 進捗

更新日: 2026-07-28

## 完了した作業

### フォルダ階層・移動モード（P9-1〜P9-8）

- Firefoxブックマークのツリーデータ契約を追加した。
- 現在フォルダの保存、復元、削除時の親またはルートへのフォールバックを実装した。
- ルートから現在フォルダまでのパンくず配列生成を実装した。
- フォルダ領域の表示と、フォルダクリックによる階層移動を実装した。
- カスタム表示順をフォルダ単位で保存・復元する形式へ移行した。
- フォルダとブックマークの表示母集団を分離した。
- 移動操作を「カスタム配置」「通常」「公式整理」の排他的な3状態に変更した。
- カスタム配置による同一フォルダ内の並べ替えを実装した。
- Firefox公式順の同階層D&Dを実装した。
- ブックマークとフォルダの公式階層移動を実装した。
- 循環、特殊ルート、検索中、API失敗に対する安全制御と表示復旧を実装した。
- 公式移動の結果通知とUndoを実装した。
- `<`と`>`によるフォルダ移動履歴の前後移動を実装した。
- 保存フォルダへの復帰時に、祖先経路をナビゲーション履歴へ復元するよう修正した。
- Firefox実機で階層移動、モード分離、復元、Undoを確認した（ユーザー確認済み）。

### ドッキングベイ

- ドッキングベイの仕様を `docs/dockingbay.md` に策定した。
- チップ、固定ベイ、ベイ工場、名前付きレイアウト、4レール配置、保存・復旧の実装フェーズをDB-1〜DB-15として定義した。
- DB-1「チップ種別と状態契約」を実装した。
  - 条件、操作、アクションの3種類を判別共用体として分離した。
  - チップ種別の分類をインスタンスではなく定義側で保持するようにした。
  - 条件チップの順次適用、後勝ち、異なる状態キーの合成を実装した。
  - 操作チップが共通状態を読み書きする契約を追加した。
  - アクションチップが明示実行時だけ動作する契約を追加した。
  - 文字ベースのテスト用チップで正常系、異常系、境界値を確認した。
  - 全48テストファイルの成功を確認した。
- DB-2「固定ベイ」を実装した。
  - 上レールへ検索ベイを固定配置した。
  - 下レールへフォルダ履歴、ソート、移動モードの3ベイを固定配置した。
  - フォルダ領域と公式移動通知はベイ外に維持した。
  - ベイ名をアクセシブルネームとして付与し、既存コントロールのIDと操作契約を維持した。
  - 狭いパネルでは下レールのベイが折り返すようにした。
  - 全49テストファイルの成功を確認した。
- DB-3A「既存表示機能の配置非依存化」を実装した。
  - 検索語、フィルタ、ソート、移動モードをまとめる共通状態コントローラーを追加した。
  - パネル本体をDOMやベイ配置に依存しない状態更新へ接続した。
  - 検索またはフィルタ開始時に移動モードを通常へ戻す連携を固定した。
  - 文字ベースのテスト用フィルタで検索、フィルタ、ソートの合成を確認した。
  - 入力データ、フィルタ、更新前状態を変更しないことを確認した。
  - 全50テストファイルの成功を確認した。
- DB-3B「訪問状態フィルタUI」を実装した。
  - 検索ベイ直後へ訪問状態フィルタベイを固定配置した。
  - 「すべて」「訪問あり」「未訪問」の排他的な3択を追加した。
  - 「訪問あり」は訪問回数が1以上、「未訪問」は訪問回数が0の項目だけを対象とした。
  - 訪問回数が欠損した項目は「訪問あり」「未訪問」のどちらにも混在させないようにした。
  - フィルタ開始時に移動モードを通常へ戻し、表示集合を即時再計算するようにした。
  - 全52テストファイルの成功を確認した。
- DB-3C「表示形式と切替UIの仕様」を策定した。
  - 初期版の表示形式を「パネル」「アイコン」「カード」「一覧」の4形式とした。
  - 未設計でカードと役割が重なる「詳細」は初期版から保留した。
  - 共通の`activeViewType`、切替UI、情報量、共通操作、状態遷移を定義した。
  - DB-3Dを状態と切替UI、一覧、アイコン、カード、連携回帰の5フェーズへ分割した。
- DB-3D-1「表示形式状態と切替UI」を実装した。
  - 共通状態へ`activeViewType`を追加し、初期値を`panel`とした。
  - 下レールのソートベイと移動モードベイの間へ表示形式ベイを配置した。
  - 「パネル」「アイコン」「カード」「一覧」の排他的な4択を追加した。
  - 表示形式を変更しても検索、フィルタ、ソート、移動モードを維持するようにした。
  - 複数の表示形式UIを同じ共有状態へ同期できる入力接続を追加した。
  - 選択値をメイン領域の`data-view-type`へ反映した。
  - 非パネル形式の描画はDB-3D-2〜DB-3D-4へ保留した。
  - 全54テストファイルの成功を確認した。
- DB-3D-2「一覧式」を実装した。
  - 共通表示集合の順序を維持したまま、アイコン、タイトル、ドメインを1行表示するようにした。
  - 一覧行へ既存の`.panel-tile`、`data-guid`、`data-url`契約を適用した。
  - クリック、カスタム配置、公式整理、先頭末尾drop境界を既存処理と共有した。
  - 440px以下ではドメインを隠し、タイトルを優先するようにした。
  - 不正URLのドメインを空表示にし、入力データを変更しないようにした。
  - 旧一覧に存在した削除ボタンは復活させなかった。
  - 全56テストファイルの成功を確認した。
- DB-3D-3「アイコン式」を実装した。
  - 共通表示集合の順序を維持した均一アイコングリッドを追加した。
  - 52pxの共通ブックマークアイコンと最大2行のタイトルを表示するようにした。
  - 86px以上の自動列、列間10px、行間20pxを基準にした。
  - 訪問回数、最終訪問日時、ドメインを常時表示しないようにした。
  - クリック、カスタム配置、公式整理、先頭末尾drop境界を既存処理と共有した。
  - アイコンタイルは縦長でも左右半分でdrop前後を判定するようにした。
  - 具体寸法の最終調整はFirefox実機確認へ保留した。
  - 全58テストファイルの成功を確認した。
- DB-3D-4「カード式」を実装した。
  - 48pxのアイコン、タイトル、ドメインを均一カードへ表示するようにした。
  - 訪問回数と最終訪問日時を省略しない独立した2行へ表示するようにした。
  - 訪問回数を実数値、未訪問、履歴不明の3状態で区別した。
  - 最終訪問日時をローカル時間の`YYYY/MM/DD HH:mm`で表示した。
  - 最終訪問日時の欠損・不正値を「記録なし」とした。
  - 省略表示時も各メタ行の全文を`title`へ保持した。
  - 最小幅270px、最小高さ96pxとし、内容に応じて高さを拡張できるようにした。
  - クリック、カスタム配置、公式整理、先頭末尾drop境界を既存処理と共有した。
  - 全60テストファイルの成功を確認した。
- DB-3D-5「4形式の連携回帰」を完了した。
  - 4形式の分岐をDOM非依存の表示形式プレゼンターへ抽出した。
  - パネル、アイコン、カード、一覧が同じ検索・フィルタ・ソート結果を使うことを固定した。
  - 選択中の表示形式だけを1回描画するようにした。
  - 0件時は全形式で共通の空状態を表示するようにした。
  - パネル式だけがグリッド寸法を待ち、他の3形式は寸法未確定でも描画するようにした。
  - カスタム配置、公式整理、通常、検索中、フィルタ中、公式移動処理中のD&D判定を共通化した。
  - 入力項目と共有状態を変更しないことを確認した。
  - 全62テストファイルの成功を確認した。
- DB-3「固定状態の機能テスト」全体を完了した。
- DB-4A「永続モデルとID発行」を実装した。
  - ベイ設定、メインレイアウト、ドッキングメタデータを3つのversion付き文書へ分離した。
  - ベイ、チップインスタンス、レイアウトの変更不能IDとデータ契約を追加した。
  - `kind`を保存せず、`chipType`に対応する定義側で保持するDB-1契約へ統一した。
  - ベイ、チップ、レイアウトへ独立した単調増加カウンターを追加した。
  - 削除済みIDを再利用せず、安全整数の上限を越えないID発行を実装した。
  - 具体的な内部デフォルト内容を注入値として扱う空文書生成を追加した。
  - 全63テストファイルの成功を確認した。
- DB-4B-1「ベイ文書外形と採番カウンターの正常化」を実装した。
  - 正しいschema versionとベイ配列を持つ文書を防御的コピーして受け入れるようにした。
  - 欠損または不正なベイ・チップ採番カウンターを`1`へ補正するようにした。
  - 未対応schema、不正な文書外形、ベイ配列の破損、採番上限到達時は注入されたフォールバックへ復旧するようにした。
  - 復旧結果とフォールバック元が状態を共有しないことを固定した。
  - 全64テストファイルの成功を確認した。
- DB-4B-2「ベイ項目の構造正常化」を実装した。
  - 正規形`bay-<連番>`かつ安全整数範囲内のIDだけを維持するようにした。
  - 重複IDは保存順で最初のベイを維持し、後続を除外するようにした。
  - 不正なベイ要素と不正IDを除外するようにした。
  - 欠損または空白だけの名前を「名称未設定」へ補正した。
  - 不正な`permanent`を`false`へ補正し、有効な名前と真偽値は維持した。
  - DB-4B-3の対象となるチップ内容は変更せず維持した。
  - 対象関数と補助関数へJSDoc、処理境界へ行コメントを追加した。
  - 全65テストファイルの成功を確認した。
- DB-4B-3「チップ項目・順序・採番カウンターの構造正常化」を実装した。
  - 正規形`chip-<連番>`かつ安全整数範囲内のインスタンスIDだけを維持するようにした。
  - チップIDの重複を文書全体で判定し、保存順で最初の有効チップを維持するようにした。
  - 空の`chipType`を除外し、未知の`chipType`はDB-14での判定まで維持するようにした。
  - JSONオブジェクトとして保存できない設定だけを`{}`へ復旧するようにした。
  - 有効order順、同値は保存順、不正orderは末尾として各ベイ内で1始まりの連続orderへ再採番した。
  - 維持したベイ・チップIDを再利用しないよう、各採番カウンターを最大連番より上へ引き上げた。
  - 次回採番が安全整数範囲を越える場合は、注入された文書全体のフォールバックへ復旧するようにした。
  - 全66テストファイルの成功を確認した。
- DB-4B「ベイ・チップの構造正常化」全体を完了した。
- DB-4C-1「レイアウト文書・項目・採番の構造正常化」を実装した。
  - schema versionとレイアウト配列を持つ文書外形を検証するようにした。
  - 正規形`layout-<連番>`かつ安全整数範囲内のIDだけを保存順で維持し、重複IDは先勝ちとした。
  - 欠損または空白だけの名前を「名称未設定」へ補正し、重複する有効名は維持した。
  - `systemDefault`を保存値ではなく注入された内部デフォルトIDから導出するようにした。
  - 内部デフォルト欠損、未対応schema、不正な文書外形、採番不能時は文書全体をフォールバックするようにした。
  - 維持したレイアウトIDを再利用しないよう採番カウンターを最大連番より上へ引き上げた。
  - 配置内容はDB-4C-2まで解釈せず、防御的コピーで維持した。
  - 全68テストファイルの成功を確認した。
- DB-4C-2「ベイ配置の構造正常化」を実装した。
  - 正常化済みベイID集合を使い、存在しないベイ参照を配置から除外するようにした。
  - 不正な配置要素と4レール以外の配置を除外するようにした。
  - 同一レイアウト内の重複ベイ参照は保存順で最初の配置を維持するようにした。
  - 配置を上、左、右、下の適用順にまとめ、有効order順、同値は保存順、不正orderは末尾へ配置した。
  - 各レール内のorderを1始まりの連続値へ再採番した。
  - ユーザーレイアウトの欠損配置配列を空配列へ部分復旧するようにした。
  - 内部デフォルトの配置に補正が必要な場合は、復旧入口を守るため文書全体を注入値へフォールバックするようにした。
  - 全69テストファイルの成功を確認した。
- DB-4C-3「ドッキングメタデータの構造正常化」を実装した。
  - schema versionとメタデータ文書外形を検証するようにした。
  - 有効なactive、最終使用、既定、内部デフォルトの順で安全なactiveレイアウトを選ぶようにした。
  - 無効な`preferredLayoutId`と`lastUsedLayoutId`だけを個別に除去し、有効な任意参照は維持した。
  - 正常化済みレイアウトに内部デフォルトが存在しない場合は、注入されたメタデータへフォールバックするようにした。
  - 正常なメタデータとフォールバック結果を防御的コピーで返し、呼び出し間で状態を共有しないようにした。
  - 全70テストファイルの成功を確認した。
- DB-4C「レイアウト・メタデータの構造正常化」全体を完了した。
- DB-4D-1「3文書のストレージキーと一括読込」を実装した。
  - `bayConfigurations.v1`、`mainLayouts.v1`、`dockingMetadata.v1`を固定キーとして定義した。
  - 3キーを1回の`browser.storage.local.get()`で読み込むようにした。
  - 未保存キーを`undefined`として返し、保存値の正常化やフォールバックをDB-4Eへ分離した。
  - 不正な保存値も解釈せずそのまま返し、ストレージ読込エラーを呼び出し元へ伝播するようにした。
  - 全71テストファイルの成功を確認した。
- DB-4D-2「単独文書のストレージ保存」を実装した。
  - ベイ設定、メインレイアウト、ドッキングメタデータごとに型付きの単独保存関数を追加した。
  - 各関数が対応する1キーだけを`browser.storage.local.set()`へ渡すようにした。
  - 保存要求前に文書全体を防御的コピーし、呼出後の入力変更が保存値へ影響しないようにした。
  - 同期・非同期の保存エラーを呼び出し元へ伝播するようにした。
  - 全72テストファイルの成功を確認した。
- DB-4D-3「複数文書の一括保存」を実装した。
  - 任意のドッキング文書パッチを受け取る共通保存関数を追加した。
  - 指定された複数文書を対応する固定キーへ写し、1回の`browser.storage.local.set()`で保存するようにした。
  - 明示的な`undefined`を保存対象から除き、空パッチではストレージ要求を発生させないようにした。
  - すべての保存対象を要求前に防御的コピーするようにした。
  - 単独保存関数も共通の一括保存経路を使うように統一した。
  - 一括保存エラーを呼び出し元へ伝播するようにした。
  - 全73テストファイルの成功を確認した。
- DB-4D「3文書のストレージ保存・読込」全体を完了した。
- DB-4E-1「3文書の依存順正常化」を実装した。
  - ストレージ副作用を持たない3文書の統合正常化関数を追加した。
  - ベイ、レイアウト、メタデータの依存順で既存正常化関数を適用するようにした。
  - 正常化済みベイID集合をレイアウト配置の参照検証へ渡すようにした。
  - 正常化済みレイアウト文書をメタデータの参照検証へ渡すようにした。
  - 正常化済み3文書、文書別の復旧状態、変更文書名を結果として返すようにした。
  - 上流文書の部分復旧が下流文書へ反映されることを固定した。
  - 全74テストファイルの成功を確認した。
- DB-4E-2「一括読込・部分復旧・自動再保存」を実装した。
  - 3文書を1回で読み込み、依存順正常化を実行する公開関数を追加した。
  - 補正またはフォールバックされた文書だけを保存パッチへ含めるようにした。
  - 全文書が正常な場合はストレージ書込みを発生させないようにした。
  - 複数文書の復旧結果を1回の`browser.storage.local.set()`で自動再保存するようにした。
  - 読込または自動再保存が失敗した場合は、成功結果を返さずエラーを伝播するようにした。
  - 全75テストファイルの成功を確認した。
- DB-4E「部分復旧とストレージ統合」全体を完了した。
- DB-4「ベイ・チップ永続モデル」全体を完了した。
- DB-5A「ベイ工場の表示モデルと静的レンダラー」を実装した。
  - 1つのベイID、名前、文字チップだけを受け取る静的表示モデルを追加した。
  - DOM非依存の描画計画と、横ベイ編集領域へ反映するレンダラーを分離した。
  - テスト用の短い文字チップ「検索」「訪問状態」「表示形式」をモデル順で描画できるようにした。
  - チップを操作可能なボタンにせず、DB-6まで静的な文字要素として扱うようにした。
  - 空ベイへ配置案内と、左側が上部・右側が下部になる縦置き案内を表示するようにした。
  - メインUIや永続ベイデータへ接続せず、注入可能な表示モデルとして分離した。
  - 全76テストファイルの成功を確認した。
- DB-5B「ベイ工場モーダルの外枠とレイアウト」を実装した。
  - 閉じた状態のネイティブ`dialog`としてベイ工場を追加した。
  - 閉じる入口を右上の`×`へ集約し、下部の閉じる・キャンセル操作を置かない構造にした。
  - ベイ名、元に戻す、やり直す、保存、削除を上部メニューバーへ仕様順で配置した。
  - DB-5時点では編集系コントロールを無効状態とし、偽のベイ名をHTMLへ埋め込まないようにした。
  - DB-6用のチップツール領域と、1ベイだけを扱う横編集領域を配置した。
  - 文字チップを横方向へ並べ、収まらない場合に編集領域内でスクロールできるようにした。
  - 最大600px、画面端12pxを基準とし、440px以下ではほぼ全画面になるレスポンシブCSSを追加した。
  - 全77テストファイルの成功を確認した。
- DB-5C「対象選択入口とベイ工場の開閉」を実装した。
  - ヘッダーへグローバルな「ベイを編集」入口を追加した。
  - 空の初期選択肢と無効状態の編集開始ボタンを持つ対象選択バーを追加した。
  - 注入されたベイ一覧から`permanent: true`を除外し、ユーザーベイだけを選択肢へ描画するようにした。
  - 選択変更を外側へ通知できるようにし、アウトライン同期の接続点を用意した。
  - 編集開始ボタン、Enter、ダブルクリックから選択した1ベイを静的レンダラーへ渡してモーダルを開くようにした。
  - 右上`×`とEscapeのcancelイベントを同じ閉鎖処理へ接続した。
  - パネル本体には空のユーザーベイ一覧を注入し、DB-8まで偽データを表示・保存しないようにした。
  - 全79テストファイルの成功を確認した。
- DB-5「ベイ工場の静的UI」全体を完了した。
- DB-6A「文字チップツールの分類と静的描画」を実装した。
  - `chipType`、3分類、表示名、説明を持つ注入可能な文字ツール表示モデルを追加した。
  - 条件、操作、アクションの固定順へ分類するDOM非依存の描画計画を追加した。
  - 空カテゴリを省略し、同一カテゴリ内では注入されたツール順を維持するようにした。
  - 短い文字ツール「検索」「訪問状態」「表示形式」などを非ドラッグのボタンとして描画するようにした。
  - ツールボタンへ`chipType`と説明をdata属性として保持し、ツールチップとD&Dの接続点を用意した。
  - パネル本体では空のツール定義を描画し、実チップや偽データをまだ注入しないようにした。
  - 全80テストファイルの成功を確認した。

- DB-6B「150msツールチップ」を実装した。
  - 文字ツールのhoverまたはfocus開始から150ms後に、表示名と説明を持つツールチップを表示するようにした。
  - 149ms時点では非表示、150ms到達時に表示される境界値をテストした。
  - マウス離脱、blur、ドラッグ開始、画面切断時に待機タイマーと表示を解除するようにした。
  - ツール内部の子要素間を移動した場合は、ツールチップを閉じないようにした。
  - 表示中のツールへ `aria-describedby` を付与し、非表示時に解除するようにした。
  - ツールチップをスクロールするツール列の外側へ配置し、長い説明は複数行へ折り返すようにした。
  - 全81テストファイルの成功を確認した。

- DB-6C「ツールからベイへのD&D追加」を実装した。
  - 文字チップツールをドラッグ可能なボタンとして描画するようにした。
  - ツールの短い表示名を使った文字ベースのドラッグプレビューを生成するようにした。
  - 空ベイは0番、既存チップ間は各チップの横方向の中心を境界として挿入位置を算出するようにした。
  - drop時に`chipType`と挿入位置だけを外側へ通知し、ID発行とドラフト更新をDB-7へ分離した。
  - 同じツールを何度でも追加でき、無関係な外部ドラッグは受け付けないようにした。
  - dragend、drop、切断時にドラッグ元の表示と一時プレビューを確実に解除するようにした。
  - 全82テストファイルの成功を確認した。

- DB-6D「ベイ内並べ替えと領域外削除」を実装した。
  - 配置済み文字チップをドラッグ可能にし、横方向の中心を境界として並べ替え位置を算出するようにした。
  - 移動元を除いた後の添字へ正規化し、同じ位置へのdropでは変更を通知しないようにした。
  - 編集キャンバス内かつ横ベイ枠外へdropした場合だけ、対象`instanceId`の削除を通知するようにした。
  - 単なるdragend、編集キャンバス外への移動、無関係な外部ドラッグでは削除しないようにした。
  - dragend、drop、切断時にドラッグ中の表示と内部状態を解除するようにした。
  - 並べ替え・削除結果は外側へ通知するだけとし、ドラフト更新をDB-7へ分離した。
  - 全83テストファイルの成功を確認した。

- DB-6E「D&D回帰と視覚状態」を実装した。
  - 新規ツール追加と配置済みチップ操作が相互に誤作動しないdrop境界を回帰テストで固定した。
  - 新規ツールは横ベイ枠または空ベイ領域だけへdropでき、編集キャンバスの余白では追加されないようにした。
  - 挿入位置を対象チップの左辺、末尾挿入を横ベイ右端、空ベイ挿入を空領域の内枠で表示するようにした。
  - 配置済みチップを横ベイ枠外へ運んだ場合、編集キャンバス背景で削除候補を表示するようにした。
  - drop、dragend、切断時に挿入・削除候補の視覚状態をすべて解除するようにした。
  - 全83テストファイルの成功を確認した。
- DB-6「チップツールセレクター」全体を完了した。
- DB-7A「編集セッションとドラフト生成」を実装した。
  - ユーザーベイの保存基準と初期ドラフトを独立した防御的コピーとして生成するようにした。
  - 空のユーザーベイも編集対象として受け入れるようにした。
  - 不明なベイIDと`permanent: true`の固定ベイを編集対象として拒否するようにした。
  - 原本文書と返却スナップショットの変更が編集セッション内部へ影響しないことを固定した。
  - 全84テストファイルの成功を確認した。
- DB-7B「追加とチップID発行」を実装した。
  - ツール種別と挿入位置から新しいチップインスタンスをドラフトへ追加するようにした。
  - 同種チップの複数追加と、先頭・中間・末尾への挿入を許可した。
  - セッション内で単調増加IDを発行し、追加後のorderを1始まりの連続値へ再採番した。
  - 注入されたチップ定義から初期設定を生成し、未登録時は空設定を使用するようにした。
  - 不正な種別・挿入位置、初期設定生成失敗、採番上限時にIDとドラフトを変更しない原子的失敗を固定した。
  - 全84テストファイルの成功を確認した。
- DB-7C「削除・並べ替え・設定変更」を実装した。
  - `instanceId`を使ったチップ削除、最終添字への並べ替え、設定全体の置換をドラフトへ追加した。
  - 削除・並べ替え後のorderを1始まりの連続値へ再採番した。
  - 同位置への並べ替えを変更なしとして扱うようにした。
  - 設定入力を防御的コピーし、保存基準と呼び出し元から独立させた。
  - 不明なチップIDと不正な移動先を原子的に拒否するようにした。
  - 同一セッションで追加後に削除したチップIDを再利用しないことを固定した。
  - 全84テストファイルの成功を確認した。
- DB-7D「Undo・Redoと保存境界」を実装した。
  - 追加、削除、並べ替え、設定変更を有効な操作1回ごとの履歴として記録するようにした。
  - Undo・Redoで各操作の前後状態を復元し、空履歴では変更なしとして扱うようにした。
  - Undo後の新規操作でRedo分岐を破棄するようにした。
  - 追加をUndoしても採番カウンターを戻さず、Redoでは同じチップIDを復元するようにした。
  - 同位置移動と失敗した操作を履歴へ追加しないようにした。
  - 保存成功状態を新しい不可逆な履歴開始点にする境界更新を追加した。
  - 全85テストファイルの成功を確認した。
- DB-7E「保存・失敗保持・二重保存防止」を実装した。
  - 元のベイ設定文書から対象ベイとチップ採番値だけを差し替えた保存文書を生成するようにした。
  - 保存対象と次回保存基準を防御的コピーし、対象外ベイとベイ採番値を維持するようにした。
  - 保存成功時だけ保存基準を更新し、Undo・Redo履歴を新しい不可逆境界へ進めるようにした。
  - 保存失敗時にドラフト、採番値、Undo・Redo履歴を維持するようにした。
  - 保存中の追加、削除、並べ替え、設定変更、Undo・Redo、二重保存を拒否するようにした。
  - 変更のないセッションではストレージ要求を発生させないようにした。
  - 全86テストファイルの成功を確認した。
- DB-7F「破棄確認とモーダル統合」を実装した。
  - 編集セッションを最後の保存成功状態へ戻し、Undo・Redo履歴を破棄する操作を追加した。
  - 変更を破棄してもセッション内のチップ採番値を戻さず、IDを再利用しないようにした。
  - 未変更時の`×`とEscapeはベイ工場を即時閉鎖するようにした。
  - 未保存時の`×`とEscapeは共通経路で「編集を続ける／変更を破棄」の専用確認表示を開くようにした。
  - 「編集を続ける」では状態を維持し、「変更を破棄」では注入された破棄処理後に閉じるようにした。
  - 確認表示へ`alertdialog`の役割を付け、表示時に「編集を続ける」へフォーカスするようにした。
  - 全86テストファイルの成功を確認した。
- DB-7G「DB-6接続・回帰」を実装した。
  - DB-6のツール追加通知と配置済みチップの並べ替え・削除通知を編集セッションへ接続する取引コントローラーを追加した。
  - 追加、並べ替え、削除、Undo、Redoを同じドラフト再描画経路へ集約した。
  - チップ種別を注入された表示名へ変換し、未知の表示名は`chipType`で安全に表示するようにした。
  - Undo・Redo・保存ボタンを履歴、未保存、保存中の状態へ同期するようにした。
  - 保存成功と保存失敗を注入コールバックへ通知し、失敗後も編集と再保存を継続できるようにした。
  - DB-8まで偽の永続ベイやツールをパネル本体へ注入しない境界を維持した。
  - 全87テストファイルの成功を確認した。
- DB-7「ベイ編集トランザクション」全体を完了した。
- DB-8A「ベイ管理ドメイン操作」を実装した。
  - 保存前だけ使う一時識別子と検証済みの名前を持つ空の新規ベイドラフトを追加した。
  - ベイ名の前後空白を除去し、空名を保存前に拒否する契約を追加した。
  - ユーザーベイの改名を原本と状態共有しない純粋操作として追加し、重複名を許可した。
  - 既存名を避けた「元名 2」以降の連番名と、正式ID未発行の独立チップ値を持つ複製素案を追加した。
  - 完全削除時に参照を除去するレイアウトIDとレイアウト数を保存順で集計するようにした。
  - 改名、複製、完全削除の各状態層で`permanent: true`の固定ベイを拒否するようにした。
  - 全88テストファイルの成功を確認した。
- DB-8B「新規ベイドラフト」を実装した。
  - ヘッダーへ「ベイを追加」入口を追加した。
  - 入口から既定名「新しいベイ」とセッション内一時識別子を持つ空のベイ工場を開くようにした。
  - 保存前ドラフトを専用コントローラー内部だけに保持し、メインUI、編集対象一覧、永続文書へ公開しないようにした。
  - 新規ベイの名前入力を有効化し、前後空白を除去した有効名をドラフトへ反映するようにした。
  - 空名では直前の有効名へ戻し、注入されたエラー通知入口へ渡すようにした。
  - 一時ドラフトの取得値を防御的コピーし、破棄操作自体は保存やダイアログ閉鎖を行わないようにした。
  - 全89テストファイルの成功を確認した。
- DB-8C「新規保存と初期配置」を実装した。
  - 最初の保存要求で一時ベイへ正式な単調増加ベイIDを発行するようにした。
  - 新規ベイを編集可能な現在レイアウトの上レール末尾へ論理配置するようにした。
  - 内部デフォルトレイアウトが現在の場合は不変契約を優先し、新規ベイを未配置で保存するようにした。
  - ベイ設定文書とメインレイアウト文書を1回の保存要求で確定するようにした。
  - 対象外ベイ、レイアウト、各採番値を維持し、入力と保存結果を防御的コピーで分離した。
  - 現在レイアウト欠損、ベイ採番上限、保存失敗時に入力を変更せず、保存要求を部分実行しないようにした。
  - 保存失敗後の再試行で、未確定の同じ候補ベイIDを使うことを固定した。
  - 全90テストファイルの成功を確認した。
- DB-8D「改名」を実装した。
  - 既存ユーザーベイの名前変更をDB-7編集セッションの1操作として追加した。
  - 前後空白を除去し、重複名を許可し、空名ではドラフトと履歴を変更しないようにした。
  - trim後に現在名と同じ場合は変更なしとし、Undo履歴を追加しないようにした。
  - Undo・Redoで名前を復元し、保存成功境界と未保存変更の破棄へ名前を含めるようにした。
  - ベイ編集取引コントローラーへ名前入力を接続し、再描画、履歴操作、保存中状態へ同期するようにした。
  - 不正名では直前のドラフト名へ入力を戻し、注入されたエラー通知入口へ渡すようにした。
  - 全91テストファイルの成功を確認した。
- DB-8E「複製」を実装した。
  - ユーザーベイの明示的な複製を保存前の「複製予定」状態として追加した。
  - 複製予定のUndo・Redoと、Undo後の同じ複製素案の復元を実装した。
  - 保存時に新しいベイIDと全所属チップの新しいインスタンスIDを連続発行するようにした。
  - チップ構成と設定を元ベイから防御的に複製し、保存後の変更が元ベイへ影響しないようにした。
  - 複製ベイを編集可能な現在レイアウトの上レール末尾へ配置し、内部デフォルトでは未配置にした。
  - ベイ設定文書とメインレイアウト文書を1回の保存要求で確定するようにした。
  - 保存失敗時に複製予定とUndo履歴を維持し、再試行で同じ候補ベイ・チップIDを使用するようにした。
  - 固定ベイ、予定なし保存、採番上限を保存前に拒否するようにした。
  - 全92テストファイルの成功を確認した。
- DB-8F「削除予定と参照集計」を実装した。
  - 完全削除を即時実行せず、対象ベイIDと参照レイアウト一覧を持つ保存前の削除予定状態として追加した。
  - 削除予定のUndo・Redoと、Undo後の同じ削除計画の復元を実装した。
  - 同一レイアウト内の配置数ではなく、削除対象を参照するレイアウト数を表示用に集計するようにした。
  - 未配置ベイを参照数0の削除予定として扱えるようにした。
  - 同じ削除予定を重ねても履歴を重複追加しないようにした。
  - 返却した削除計画と内部予定状態を防御的コピーで分離した。
  - 不明なベイと`permanent: true`の固定ベイを予定化前に拒否するようにした。
  - 全93テストファイルの成功を確認した。
- DB-8G「完全削除トランザクション」を実装した。
  - 削除予定の保存時に対象ベイ定義と全レイアウトの配置参照を候補文書から除去するようにした。
  - 参照除去後の各レール内orderを1始まりの連続値へ再採番するようにした。
  - ベイ設定文書とメインレイアウト文書を1回の保存要求で確定するようにした。
  - ベイ・チップ・レイアウトの採番値と対象外データを変更せず維持するようにした。
  - 保存成功後は削除完了状態へ進み、削除予定とUndo・Redo履歴を破棄するようにした。
  - 保存失敗時は削除予定、参照集計、Undo可能状態を維持するようにした。
  - 保存中の予定・Undo・Redo・二重保存を拒否するようにした。
  - 入力文書、保存要求、成功結果を防御的コピーで分離した。
  - 全93テストファイルの成功を確認した。
- DB-8H「UI・選択肢・回帰統合」を実装した。
  - ベイ工場メニューへ合意した順序で「複製」を追加した。
  - ロード済みユーザーベイ一覧を保存後に差し替えられる選択コントローラーへ拡張した。
  - 追加・改名・複製後は同じベイIDの選択を維持し、削除後は無効な選択を解除するようにした。
  - 工場を開いたベイIDをDB-7編集セッションへ接続できる開始通知を追加した。
  - 複製・削除ボタンをDB-8E/8F管理セッションへ接続できる明示コールバックへ追加した。
  - 削除予定時に参照レイアウト数を表示し、削除保存後は「ベイを削除しました」の完了表示へ切り替えるようにした。
  - 削除完了表示では編集メニューと作業領域を隠し、別ベイ開始時に通常編集表示へ戻せるようにした。
  - DB-6操作、改名、Undo・Redo、保存成功・失敗の既存連携回帰を維持した。
  - 内部デフォルト文書の実注入を、名前付きレイアウトを確定するDB-9との統合境界として維持した。
  - 全94テストファイルの成功を確認した。
- DB-8「ユーザーベイ管理」全体を完了した。
- DB-9A「レイアウト名とID発行」を実装した。
  - レイアウト名の前後空白を除去し、空白だけの名前を拒否するようにした。
  - 重複名へ`名前 (2)`形式で空いている最小の連番を自動付与するようにした。
  - 既に括弧付き連番を持つ重複名では、基底名から次の空き番号を解決するようにした。
  - 共通の単調増加ID発行契約を利用し、名前とレイアウトIDを1つの候補として発行するようにした。
  - 不正な採番値と安全整数上限到達を候補生成前に拒否するようにした。
  - 全95テストファイルの成功を確認した。
- DB-9B「空白からの新規作成」を実装した。
  - 唯一の`permanent`ベイだけを上レール先頭へ配置した名前付きレイアウト候補を生成するようにした。
  - ユーザーベイを新規レイアウトへ暗黙に配置せず、未配置のまま維持するようにした。
  - DB-9Aの名前・ID発行契約を再利用し、レイアウト文書の採番値と一覧を更新するようにした。
  - 固定ベイの欠損または複数存在を候補生成前に拒否するようにした。
  - 入力文書、返却レイアウト、返却文書を防御的コピーで分離した。
  - 全96テストファイルの成功を確認した。
- DB-9C「同じベイを使う複製」を実装した。
  - 複製元の全配置を同じベイID、レール、順序のまま参照する新レイアウト候補を生成するようにした。
  - 通常の名前付きレイアウトに加え、内部デフォルトレイアウトも複製元として扱えるようにした。
  - 複製元が内部デフォルトでも、新レイアウトへ`systemDefault`保護属性を引き継がないようにした。
  - 新しいレイアウトIDだけを発行し、ベイ・チップの採番値を変更しない境界を維持した。
  - 不明な複製元と採番上限を候補生成前に拒否し、配置を防御的コピーで分離した。
  - 全97テストファイルの成功を確認した。
- DB-9D「ベイも複製」を実装した。
  - 複製元に配置されたユーザーベイへ配置順で新しいベイIDを発行し、所属チップへ新しいインスタンスIDを発行するようにした。
  - `permanent`ベイは複製せず、元と同じベイIDを新レイアウトから参照するようにした。
  - 同じユーザーベイ参照は1回だけ複製し、新レイアウト内の参照を新IDへ置換するようにした。
  - ベイ名、チップ種別、順序、設定をディープコピーし、複製後の変更を元データから分離した。
  - 内部デフォルトも複製元にでき、保護属性は新レイアウトへ引き継がないようにした。
  - 不明な配置参照とレイアウト・ベイ・チップの採番上限を候補生成前に拒否するようにした。
  - 全98テストファイルの成功を確認した。
- DB-9E「改名と既定指定」を実装した。
  - 名前付きレイアウトの改名へDB-9Aの空白除去と重複名連番規則を適用した。
  - 改名対象自身を重複判定から除外し、同じ名前への据え置きを許容するようにした。
  - 名前付きレイアウトを`preferredLayoutId`へ設定し、未指定操作で既定指定だけを解除するようにした。
  - 既定指定・解除時に`activeLayoutId`と`lastUsedLayoutId`を変更しないようにした。
  - 内部デフォルトの改名・既定指定と、不明なレイアウトへの操作を状態層で拒否するようにした。
  - 入力レイアウト文書とメタデータ文書を防御的コピーで維持した。
  - 全99テストファイルの成功を確認した。
- DB-9F「選択・切替とデフォルト復帰」を実装した。
  - 名前付きレイアウトへの通常切替で`activeLayoutId`と`lastUsedLayoutId`を同時更新するようにした。
  - 既に選択中の名前付きレイアウトを再選択でき、既定指定を変更しないようにした。
  - 内部デフォルトを通常切替経路から除外し、専用の復旧操作だけで選択できるようにした。
  - デフォルト復帰では`activeLayoutId`だけを内部デフォルトへ変更し、最終使用と既定指定を維持するようにした。
  - 内部デフォルトの欠損または複数存在と、不明な通常切替先を状態層で拒否するようにした。
  - 全100テストファイルの成功を確認した。
- DB-9G「削除とフォールバック」を実装した。
  - 名前付きレイアウトだけを削除し、採番値と対象外レイアウトを維持するようにした。
  - 削除後もactiveが有効なら現在の選択を維持するようにした。
  - activeが無効になった場合は、有効な最終使用、既定、内部デフォルトの順で切替先を選ぶようにした。
  - 削除または既に無効な`lastUsedLayoutId`と`preferredLayoutId`を除去し、既定を別レイアウトへ自動移譲しないようにした。
  - 最終の名前付きレイアウトを削除しても内部デフォルトで稼働できる候補文書を生成するようにした。
  - 内部デフォルトの削除、不明な対象、内部デフォルトの欠損・複数存在を拒否するようにした。
  - 全101テストファイルの成功を確認した。
- DB-9H「保存トランザクション」を実装した。
  - レイアウト管理候補を変更対象のドッキング文書キーだけステージできる保存セッションを追加した。
  - 改名などの単一文書、削除などの2文書、独立ベイ複製などの3文書をそれぞれ1回の保存要求で確定できるようにした。
  - 保存成功時だけステージ済み文書をセッション正本へ反映し、対象外文書を維持するようにした。
  - 保存失敗時は正本を変更せず同じ候補を保持し、同一内容で再試行できるようにした。
  - 保存アダプター、ステージ値、正本スナップショットの状態共有を防御的コピーで遮断した。
  - 空の保存、保存中の再ステージ、二重保存を拒否するようにした。
  - 全102テストファイルの成功を確認した。
- DB-9I「内部デフォルト実文書」を実装した。
  - 検索、訪問状態、フォルダ履歴、ソート、表示形式、移動モードの6基本チップを持つ唯一の`permanent`ベイを定義した。
  - 固定ベイを上レール先頭へ配置する唯一の内部デフォルトレイアウトを定義した。
  - 初期メタデータのactiveを内部デフォルトへ設定し、ベイ・チップ・レイアウトの次回採番値を使用済みIDより後へ設定した。
  - 初期状態と全体復旧で同じ具体的な3文書を生成できる関数を追加した。
  - 呼び出しごとに独立した文書を返し、テンプレートへの状態共有を防止した。
  - 具体的な内部デフォルト3文書が既存の完全正常化処理で変更なしとして受理されることを確認した。
  - 全103テストファイルの成功を確認した。
- DB-9J「パネル初期ロード統合」を実装した。
  - パネル起動用ブートストラップへ具体的な内部デフォルト3文書を注入するようにした。
  - 既存の一括ロード・依存順正常化・変更文書だけの再保存をパネル`main()`の先頭へ接続した。
  - ロード済みベイをベイ工場表示モデルへ変換し、`permanent`属性を維持したまま選択コントローラーへ反映するようにした。
  - 基本6チップを日本語表示名へ変換し、未知のチップ種別は`chipType`を安全な表示名として維持するようにした。
  - 正常化済みメタデータからactiveレイアウトを解決し、後続の動的レール再構築が参照できるDOM境界へ設定した。
  - 初期ロードまたは補正保存の失敗を既存のパネルエラー表示経路へ伝播するようにした。
  - 全104テストファイルの成功を確認した。
- DB-9K「管理UIと回帰統合」を実装した。
  - ヘッダーへ名前付きレイアウト選択、専用デフォルト復帰、レイアウト管理入口を追加した。
  - 管理ダイアログへ空白または内部デフォルトを含む複製元、共有・独立の複製方式、作成、改名、既定、削除を追加した。
  - DB-9A〜9Hの純粋操作と保存セッションを管理コーディネーターで統合し、成功後だけ共通状態を更新するようにした。
  - 作成後は新レイアウトへ切り替え、通常選択肢には内部デフォルトを表示しないようにした。
  - 保存後に名前付きレイアウト選択、activeレイアウトDOM境界、ベイ工場のユーザーベイ一覧を同期するようにした。
  - 保存失敗を管理ダイアログへ表示し、保持した同じ候補を明示ボタンで再試行できるようにした。
  - 全106テストファイルの成功を確認した。
- DB-9L「完了確認」を実施した。
  - 作成、共有・独立複製、改名、削除、既定指定、通常切替、デフォルト復帰、起動復元の完了条件を実装とテストへ対応付けて確認した。
  - ベイ設定、メインレイアウト、メタデータが変更対象キーだけで保存され、独立ベイ複製だけが3文書を同時更新することを確認した。
  - 内部デフォルトの改名・削除・既定指定・通常選択が状態層とUI経路の両方で禁止されることを確認した。
  - activeレイアウトIDを後続の動的レール描画へ渡し、4レール描画をDB-10、配置編集と未配置化をDB-11へ残す境界を確認した。
  - 全106テストファイルの成功と、Firefox拡張lintのエラー・警告・通知0件を確認した。
- DB-9「名前付きレイアウト」全体を完了した。
- DB-10A「4レール描画計画」を実装した。
  - メタデータのactiveレイアウトから上、左、右、下の適用順で常に4レールを持つ描画計画を生成するようにした。
  - 各レールの配置をorder順、各ベイのチップをorder順に並べるようにした。
  - 上下レールを横向き、左右レールを縦向きとして描画計画へ明示するようにした。
  - 未配置ベイを描画計画へ含めず、不明な配置参照だけを理由付きでスキップして他のベイを維持するようにした。
  - activeレイアウトを解決できない場合は描画前に拒否するようにした。
  - 入力3文書と返却したベイ名・チップ設定を防御的コピーで分離した。
  - 全107テストファイルの成功を確認した。
- DB-10B「レール用CSS Grid骨格」を実装した。
  - 上、左、中央、右、下を明示する5領域のCSS Grid骨格を追加した。
  - 既存のフォルダ、公式整理通知、ブックマーク表示を中央領域へ維持した。
  - 左右レールを縦方向、上下レールを横方向へ展開できるDOM境界を追加した。
  - 空の左右レールを非表示にし、中央列へ残り幅を安全に割り当てるようにした。
  - 既存の固定ベイDOMと操作要素のID・順序を維持した。
  - 全108テストファイルの成功を確認した。
- DB-10C「チップ描画レジストリ」を実装した。
  - 検索、訪問状態、フォルダ履歴、ソート、表示形式、移動モードの基本6種を型付きレジストリへ登録するようにした。
  - 描画計画のチップ順を維持して各描画関数を実行し、成功したインスタンスIDを報告するようにした。
  - 未知のチップ種別は対象チップだけを理由付きでスキップし、後続描画を継続するようにした。
  - 個別描画関数またはDOM追加の例外を対象チップへ隔離し、同じベイの後続描画を継続するようにした。
  - 描画関数へ渡す設定を防御的コピーし、描画処理から元の計画を変更できないようにした。
  - 全109テストファイルの成功を確認した。
- DB-10D「横ベイ描画」を実装した。
  - 上・下レールの描画計画から横向きベイDOMをベイ順どおり生成するようにした。
  - 各ベイのチップ外枠をチップ順どおり生成し、操作部品の生成をDB-10Cのレジストリへ委譲するようにした。
  - ベイID、チップインスタンスID、チップ種別をDOM境界へ保持するようにした。
  - ベイ名と基本6チップの元の機能名を`aria-label`へ保持するようにした。
  - 未知種または個別描画エラーのチップ外枠を残さず、同じベイと後続ベイの描画を継続するようにした。
  - 縦レール計画を横描画境界で拒否し、向きの混在を防止するようにした。
  - 全110テストファイルの成功を確認した。
- DB-10E「縦ベイのクリップ描画」を実装した。
  - 左・右レールの描画計画から縦向きベイDOMを上から下の順で生成するようにした。
  - 横向きチップ本体を回転せず、固定幅viewportから超過する右側だけをクリップするようにした。
  - 左端をチップ本体の表示始点として維持し、縦用部品でも元の横置き短縮形を使うようにした。
  - 右レールをGrid右端へ固定し、LTRのチップ始点をレール内側の左方向へ展開するようにした。
  - 縦表示でも基本6チップの元の機能名を`aria-label`へ維持するようにした。
  - 未知種と個別描画エラーを対象チップへ隔離し、後続チップとベイの描画を継続するようにした。
  - 全111テストファイルの成功を確認した。
- DB-10F「共有状態への複数チップ接続」を実装した。
  - 同種の各チップDOMを独立したbindingとして1つの共有状態へ接続する汎用ファクトリーを追加した。
  - どの複製から届いたイベントでも共有状態を更新し、接続中の全複製へ同じ状態を即時同期するようにした。
  - 状態更新後に接続された複製へ、その時点の共有状態を初回描画するようにした。
  - 各複製の個別解除とファクトリー単位の全解除を分離し、再構築時に接続を破棄できるようにした。
  - 同期描画に失敗した複製だけを切断し、他の複製への同期を継続するようにした。
  - 初期値、外部更新値、取得値、各描画値を防御的コピーし、DOM接続から共有状態を変更できないようにした。
  - 全112テストファイルの成功を確認した。
- DB-10G「activeレイアウト切替と再構築」を実装した。
  - 旧動的接続の解除、動的レール消去、一時状態初期化、新active描画の順で再構築するコントローラーを追加した。
  - 初回構築と切替のどちらでも動的レール消去を正確に1回だけ実行するようにした。
  - 検索空、訪問状態すべて、パネル表示、訪問回数降順、通常移動を共通表示状態の初期値から生成するようにした。
  - フォルダ履歴を現在フォルダだけから再作成し、公式Undo、公式移動中、フォルダ移動中、D&D、保存中の一時状態を解除する契約を追加した。
  - 現在フォルダ、フォルダ別カスタム順序、ドッキング永続文書をコントローラーの所有外に置き、切替で変更しないようにした。
  - 再構築後の描画計画を防御的コピーで返し、描画結果から入力文書を変更できないようにした。
  - 全113テストファイルの成功を確認した。
- DB-10H「間隔・スクロール・端パン基盤」を実装した。
  - 利用可能幅または高さとベイ実寸から、8px、4px、2pxの順で収容可能な間隔を選ぶ計算を追加した。
  - 2px間隔でも収まらない場合だけスクロールを必要とする計画を返すようにした。
  - 上下レールを横方向のみ、左右レールを縦方向のみスクロール可能にするCSSを追加した。
  - ポインターの端からの距離に比例し、レール外では最大値に制限される符号付き端パン量を追加した。
  - 横レールでは横軸だけ、縦レールでは縦軸だけへ端パン量を適用するようにした。
  - 不正な寸法、範囲、閾値を計算境界で拒否するようにした。
  - 全114テストファイルの成功を確認した。
- DB-10I「固定DOM撤去と統合回帰」を実装した。
  - 上、左、右、下レールから検索、訪問状態、フォルダ履歴、ソート、表示形式、移動モードの固定操作DOMを撤去した。
  - 基本6チップの実DOM、イベント接続、共有状態同期、全解除を扱う動的runtimeを追加した。
  - radio group名へチップインスタンスIDを含め、同種チップを複数配置してもDOMの排他制御が衝突しないようにした。
  - 起動時とレイアウト管理成功時にactive描画計画から4レールを再構築し、旧runtimeとlistenerを破棄するようにした。
  - 動的チップ操作を既存の表示状態、フォルダ履歴、公式整理、ビュー描画へ接続し、全複製へ状態を同期するようにした。
  - 描画後のベイ実寸から8px、4px、2pxの間隔とスクロール要否を各レールへ反映するようにした。
  - 固定DOM前提の回帰テストを動的生成・アクセシビリティ契約へ更新し、全115テストファイルの成功を確認した。
  - Firefox拡張lintのエラー、警告、通知0件を確認した。
- DB-10J「完了確認」を実施した。
  - active文書から上、左、右、下の順で4レールを解決し、レール内のベイ順とチップ順を維持することを描画計画テストへ対応付けた。
  - 上下の横描画、左右の回転なし左端クリップ、右レールの左方向展開、元の機能名維持をDOM・CSSテストへ対応付けた。
  - 同種チップを独立DOMとして複数生成し、どの複製から操作しても共有状態を全複製へ同期することをruntimeテストへ対応付けた。
  - active切替時の旧接続解除、一時状態初期化、現在フォルダとカスタム順序の維持、新計画描画を再構築テストへ対応付けた。
  - 8px、4px、2pxの間隔選択、片軸スクロール、向き別端パン量をoverflowテストへ対応付けた。
  - 未知チップと個別描画例外を対象チップだけへ隔離し、後続チップとベイを継続することをレジストリ・レール描画テストへ対応付けた。
  - チップ設定の適用順と状態合成をDB-13、編集D&Dへの端パン接続をDB-11・DB-12、リサイズ・ズームを含むFirefox実機確認をDB-15の境界として確認した。
  - 全115テストファイルの成功と、Firefox拡張lintのエラー、警告、通知0件を最終確認した。
- DB-10「4レール描画」全体を完了した。
- DB-11A「ベイピッカー表示モデル」を実装した。
  - activeレイアウトのベイを、未配置はベイ文書の登録順、配置済みは上、左、右、下の適用順と各レール内のorder順で分類するようにした。
  - 同名ベイでも内部IDで区別でき、恒久デフォルト属性と配置済みベイのレール・orderをタグ情報へ保持するようにした。
  - 同じベイが複数箇所に配置されている場合は最初の適用箇所だけを採用し、後続を理由付きで無視するようにした。
  - 不明なベイ参照を理由付きで無視して他のタグを維持し、activeレイアウトを解決できない場合は表示モデル生成前に拒否するようにした。
  - 入力文書と返却したタグ情報を防御的コピーで分離した。
  - 全116テストファイルの成功を確認した。
- DB-11B「編集モード入口と操作ガード」を実装した。
  - ヘッダーへ「レイアウト編集」の入口を追加し、編集中はactiveレイアウト名と終了操作だけを持つ編集バーへ切り替えるようにした。
  - 内部デフォルトでは入口を無効化し、名前付きレイアウトへの切替が必要な理由を表示するようにした。
  - 編集開始時にレイアウト切替・管理、ベイ追加・編集を無効化し、ブックマーク・フォルダ表示をinertにするようにした。
  - 動的レールを後続の配置操作に残したまま、通常チップのイベント接続だけを編集開始時に解除するようにした。
  - 終了時に各操作の元の無効状態を復元し、保存済み文書からactiveレイアウトを再構築するようにした。
  - 編集中の永続文書差し替えと、解決不能なactiveレイアウトを拒否するようにした。
  - 全118テストファイルの成功を確認した。
- DB-11C「タグ・空状態DOM」を実装した。
  - 編集バー直下、4レールの上へ未配置・配置済みの2区分を持つベイピッカーを追加した。
  - 各区分を独立した横スクロール行にし、狭いパネルでもタグを折り返さず参照できるようにした。
  - タグへベイ名、`bay-`を省いた短縮内部ID、配置済みレールの日本語バッジを表示するようにした。
  - 恒久デフォルトベイへ「デフォルト」バッジを付け、通常ベイと同じタグとして残すようにした。
  - 同名ベイを内部IDで区別し、DB-11Aの登録順・適用順をDOM順へ維持するようにした。
  - 未配置または配置済みが0件でも区分を消さず、「該当するベイはありません」を表示するようにした。
  - 編集開始時に保存済み文書からタグを描画し、終了時にベイピッカーを非表示へ戻すようにした。
  - 全120テストファイルの成功を確認した。
- DB-11D「インメモリ配置ドラフトとクリック自動配置」を実装した。
  - 保存済み3文書を防御的コピーした配置ドラフトを生成し、編集途中の配置変更を永続状態から分離するようにした。
  - 未配置ベイの自動配置候補を上、下、左、右の固定優先順で評価するようにした。
  - 既存ベイ実寸、候補ベイ実寸、ベイ間2pxを合計し、スクロールなしで収まる最初のレールだけを採用するようにした。
  - 配置先レールの最大orderに1を加え、既存のorderが連続していなくても末尾へ配置するようにした。
  - 全レールで収容できない場合は未配置を維持し、配置済み・不明ベイの再配置を行わないようにした。
  - 4レールの寸法不足と、有限でない値または負の値を副作用前に拒否するようにした。
  - ドラフトのスナップショットを防御的コピーで返し、破棄時に保存済み配置へ完全に戻すようにした。
  - 実寸プレビューとクリックイベントの接続を、プレビューDOMを生成するDB-11Eの境界として分離した。
  - 全121テストファイルの成功を確認した。
- DB-11E「不活性ベイプレビュー」を実装した。
  - 配置ドラフトの4レールを、共有表示状態やイベントへ接続しない静的な基本6チップで実体描画するようにした。
  - 上下候補を横ベイ、左右候補を回転なしの縦ベイとして、通常描画と同じベイDOM境界へ生成するようにした。
  - プレビューベイを`aria-hidden`、`inert`、`pointer-events: none`として通常操作と支援技術の対象外にした。
  - 候補ベイを各レールへ一時描画し、候補レールの向きへ切り替えた実寸と既存ベイ実寸を取得後にDOMから除去するようにした。
  - タグクリック時に実寸をDB-11Dへ渡し、配置成立時だけドラフト、4レールプレビュー、未配置・配置済みタグを同期更新するようにした。
  - 全レールで収容できないタグクリックではドラフトと表示を変更しないようにした。
  - 編集終了時に配置ドラフトを破棄し、保存済みactiveレイアウトの通常チップ接続を再構築するようにした。
  - 全122テストファイルの成功を確認した。
- DB-11F「ベイタグD&Dライフサイクル」を実装した。
  - 未配置・配置済みの全ベイタグをネイティブD&Dのドラッグ元にするようにした。
  - ドラッグ状態へベイIDと、配置済みの場合だけ元レールを保持し、後続drop処理から参照できるようにした。
  - 配置済みタグのドラッグ開始時に元レールのプレビューをDOMへ残し、専用クラスで不透明度35%にするようにした。
  - 未配置タグのドラッグでは既存レールのプレビュー表示を変更しないようにした。
  - `dragend`、Escape、明示キャンセル、接続解除のすべてでタグと元プレビューの表示・内部状態を復元するようにした。
  - 編集セッション外ではドラッグ状態を開始せず、Firefoxが転送データ設定を拒否しても内部ベイIDで継続できるようにした。
  - このフェーズではdropによる配置変更を行わず、元の配置ドラフトを維持するようにした。
  - 全123テストファイルの成功を確認した。
- DB-11G「レール末尾配置・再配置と端パン接続」を実装した。
  - 未配置ベイを指定レール末尾へ配置し、配置済みベイを同一または別レール末尾へ移すドラフト操作を追加した。
  - 移動元の配置を先に除去して重複を防ぎ、移動先レールの最大orderに1を加えて末尾orderを決定するようにした。
  - 上、左、右、下の4レールを粗い末尾drop領域として接続し、dragover中の対象レールを破線枠で表示するようにした。
  - drop時にベイIDと対象レールをドラフトへ反映し、4レールプレビューとベイピッカーを同期再描画するようにした。
  - DB-10Hの端パン計算を接続し、上下レールは横軸だけ、左右レールは縦軸だけをポインター位置に応じてパンするようにした。
  - drop、dragleave、編集終了、明示解除時にdropマーカーとドラッグ元の半透明表示を解除するようにした。
  - 外部ドラッグではdropも端パンも開始せず、不明ベイをドラフト変更なしで拒否するようにした。
  - 全124テストファイルの成功を確認した。
- DB-11H「パネル統合・回帰確認」を実施した。
  - 編集入口、通常操作ガード、2行ピッカー、クリック自動配置、不活性プレビュー、タグD&D、レール末尾drop、端パン、終了時破棄をパネル経路へ接続して確認した。
  - ブックマーク初期復元の完了前は編集入口を無効にし、終了時に通常レールを再構築できないタイミングで編集を開始しないようにした。
  - 編集中だけ空レールへ最小drop面積を確保し、`:empty`によって未配置レールがdrop不能になる問題を解消した。
  - ベイタグを通常フォーカス可能な非ボタン要素へ変更し、スコープ外としたキーボード配置を暗黙に追加しないようにした。
  - 内部デフォルトでは初期復元後も編集入口と理由表示を維持し、恒久デフォルトベイは名前付きレイアウト内で通常タグとして配置・再配置できることを確認した。
  - ドラフトは保存APIへ接続せず、編集終了時に必ず破棄して保存済みactiveレイアウトを再描画する境界を確認した。
  - 精密な挿入・並べ替え、中央ゴミ箱への未配置化、Undo・Redo、保存と失敗復旧をDB-12へ残す境界を再確認した。
  - 全124テストファイルの成功と、Firefox拡張lintのエラー、警告、通知0件を確認した。
- DB-11I「完了確認」を実施した。
  - 未配置・配置済みの分類、登録順・適用順、同名ベイのID識別、空状態を表示モデル・ピッカーDOMテストへ対応付けた。
  - 名前付きレイアウトだけの編集入口、内部デフォルトの禁止理由、通常操作ガード、初期復元完了後の有効化を編集モードテストへ対応付けた。
  - 共有状態へ接続しない実体プレビュー、候補レール別の横・縦切替、`aria-hidden`・`inert`・ポインター無効化をプレビューテストへ対応付けた。
  - クリック時の上、下、左、右優先、2pxでの収容判定、全候補不適合時の未配置維持、同一ベイIDの重複防止を配置ドラフトテストへ対応付けた。
  - 未配置・配置済みタグのD&D、元プレビューの35%表示、Escape・キャンセル時の復元をドラッグライフサイクルテストへ対応付けた。
  - 4レール末尾への配置・同一/別レール再配置、横・縦の片軸端パン、drop状態解除をレールdropテストへ対応付けた。
  - 編集中の変更がインメモリだけに留まり、終了時に破棄して保存済みactiveレイアウトへ戻ることを編集モードと配置ドラフトの境界へ対応付けた。
  - 中央ゴミ箱による未配置化、精密な挿入・並べ替え、Undo・Redo、保存・失敗復旧はDB-12、実機のD&D・ズーム・大量ベイ確認はDB-15の対象として残した。
  - 全124テストファイルの成功と、Firefox拡張lintのエラー、警告、通知0件を最終確認した。
- DB-11「ベイピッカー」全体を完了した。
- DB-12A「配置ドラフトの任意位置挿入・並べ替え」を実装した。
  - 移動元を除外した対象レールの0始まり挿入位置へ、未配置ベイを任意位置で配置できるようにした。
  - 配置済みベイを同一レール内または別レールへ移動し、移動元と移動先のorderを1始まりの連続値へ再採番するようにした。
  - 同一レールの実質的に同じ位置への移動を`same-position`として変更なしで返すようにした。
  - 不明なベイを変更なしで拒否し、整数でない位置または範囲外位置を副作用前に拒否するようにした。
  - 入力文書とactive以外のレイアウトを変更せず、同じベイIDを1回だけ配置することを確認した。
  - 全124テストファイルの成功を確認した。
- DB-12B「Undo・Redo履歴と未保存判定」を実装した。
  - クリック自動配置、粗いレール末尾drop、任意位置の配置・並べ替えを、成功した操作ごとに1履歴として記録するようにした。
  - Undo・Redoで配置ドラフト文書全体を防御的スナップショットから復元し、履歴の両端では変更なしを返すようにした。
  - 初期保存状態との一致から未保存状態を判定し、保存済み状態までUndoした場合に未保存判定を解除するようにした。
  - Undo後の新しい変更でRedo枝を破棄し、実質的に同じ位置、不明ベイ、配置不能などの無変化操作では履歴とRedo枝を変更しないようにした。
  - discard時に保存基準へ戻し、Undo・Redo履歴も空にするようにした。
  - 履歴スナップショットと外部へ返す文書の状態共有を防御的コピーで遮断した。
  - 全124テストファイルの成功を確認した。
- DB-12C「精密なレール挿入境界とdrop表示」を実装した。
  - 横レールは各ベイの左右中心、縦レールは上下中心を境界として0始まりの挿入位置を算出するようにした。
  - 配置済みベイの並べ替えではドラッグ元を候補DOMから除外し、残った並びに対する挿入位置を返すようにした。
  - 中心点ちょうどを対象ベイの後方、空レールを位置0とする境界値を固定した。
  - 横レールは左右の縦線、縦レールは上下の横線として挿入候補を表示し、空レールは破線枠で表示するようにした。
  - dragleave、drop、明示解除、接続解除時に全向きの挿入表示を解除するようにした。
  - 精密drop中も上下は横軸、左右は縦軸だけへ既存の端パンを適用し、外部ドラッグでは開始しないようにした。
  - 既存の粗い末尾dropからパネル接続を切り替える処理はDB-12Hへ残した。
  - 全125テストファイルの成功を確認した。
- DB-12D「中央ゴミ箱表示と未配置化」を実装した。
  - 編集中の内部ベイドラッグ開始時だけ、`inert`対象の中央コンテンツ外からグリッド中央へ重なるゴミ箱を表示するようにした。
  - ゴミ箱へのdragoverを有効なdrop候補として強調し、dragleaveで強調だけを解除するようにした。
  - drop時にactiveレイアウトから対象ベイの配置だけを除外し、移動元レールのorderを1始まりへ再採番するようにした。
  - `permanent: true`の恒久デフォルトベイも通常ベイと同じ未配置化経路で扱うようにした。
  - 未配置化してもベイ定義、チップ、設定を維持し、成功した未配置化を1履歴としてUndoできるようにした。
  - 不明ベイと既に未配置のベイはドラフト、履歴、未保存状態を変更せず拒否するようにした。
  - drop、dragend、Escape、明示解除、編集終了、接続解除時にゴミ箱の強調と表示を解除するようにした。
  - 外部ドラッグではゴミ箱を表示せず、dropも受け付けないようにした。
  - 全126テストファイルの成功を確認した。
- DB-12E「編集バーのUndo・Redo・保存・削除UI」を実装した。
  - activeレイアウト名、未保存表示、元に戻す、やり直す、保存、削除、編集終了を仕様順で編集バーへ配置した。
  - 配置ドラフトの`canUndo`、`canRedo`、`dirty`から履歴ボタン、保存ボタン、未保存表示を同期する編集バーコントローラーを追加した。
  - Undo・Redo後に配置ドラフト、4レールプレビュー、ベイピッカー、編集バーを同じ再描画経路で同期するようにした。
  - クリック配置、レールdrop、中央ゴミ箱drop後も同じ経路から履歴・未保存表示を更新するようにした。
  - 保存中状態ではUndo、Redo、保存、削除、編集終了をすべて無効化し、各イベント処理も拒否する契約を追加した。
  - 保存と削除を後続の永続処理へ接続できる明示コールバックを追加した。
  - 編集終了時に編集バー接続を解除し、次の編集セッションへlistenerを残さないようにした。
  - 保存セッションと削除処理の実接続はDB-12F以降へ残した。
  - 全127テストファイルの成功を確認した。
- DB-12F「保存セッション、成功時基準化、失敗再試行」を実装した。
  - 配置ドラフトを既存のレイアウト保存セッションへ接続し、active配置を含むメインレイアウト文書だけを保存するようにした。
  - 保存成功時に現在ドラフトを新しい保存基準とし、Undo・Redo履歴と未保存表示を空にするようにした。
  - 保存成功後も編集モード、4レールプレビュー、ベイピッカー、編集バーを維持し、続く変更を新しい履歴として扱うようにした。
  - 保存済み文書を編集モードと既存レイアウト管理コーディネーターの正本へ同期し、編集終了や後続管理操作で古い配置が復活しないようにした。
  - 保存失敗時はドラフト、Undo・Redo履歴、未保存状態、保存候補を維持するようにした。
  - 保存アダプターから候補を変更されても同じメインレイアウト候補を防御的コピーから明示再試行するようにした。
  - 保存中は配置、並べ替え、未配置化、Undo、Redo、終了、再保存、再試行を停止し、ピッカーと4レールも不活性化するようにした。
  - 保存結果を編集バーの保存中、成功、失敗表示へ反映し、失敗時だけ「保存を再試行」を表示するようにした。
  - 保存要求がないクリーン状態ではストレージへ書き込まないようにした。
  - 全128テストファイルの成功を確認した。
- DB-12F-1「保存失敗候補とドラフトの分岐防止」を実装した。
  - 保存失敗候補の再試行前にドラフトを変更すると、保存候補と保存済み基準が不一致になる完了監査上の問題を修正した。
  - 失敗候補がある間は配置、並べ替え、未配置化、Undo、Redo、通常保存、削除をセッション層とUI層の両方で停止するようにした。
  - 失敗候補待ちでも「保存を再試行」と「編集を終了」からの変更破棄は利用できるようにした。
  - 再試行成功後は候補と同一のドラフトを新しい保存基準にし、通常編集を再開するようにした。
  - 再試行待ちのまま変更を破棄して終了した場合、ピッカーと4レールの`inert`状態を次回編集へ残さないようにした。
  - 全129テストファイルの成功と、Firefox拡張lintのエラー、警告、通知0件を再確認した。
- DB-12G「未保存終了確認と破棄経路」を実装した。
  - 未保存状態で「編集を終了」を選んだ場合、編集状態を維持したまま「編集を続ける／変更を破棄」の確認を表示するようにした。
  - 「編集を続ける」では確認だけを閉じ、配置ドラフト、履歴、4レール、ピッカー、未保存状態を維持するようにした。
  - 「変更を破棄」では既存の編集終了経路を通し、ドラフトと履歴を破棄して保存済みactiveレイアウトを再構築するようにした。
  - 未保存変更がない場合は確認を表示せず即時終了するようにした。
  - 保存成功後の新しい基準状態では未保存確認を表示せず終了できるようにした。
  - 編集開始時と終了時に前回の確認表示を残さないようにした。
  - 全128テストファイルの成功を確認した。
- DB-12H「パネル統合・既存粗drop置換」を実装した。
  - パネルの粗いレール末尾drop接続を精密挿入drop接続へ置換し、算出した0始まり位置を配置ドラフトの任意位置APIへ渡すようにした。
  - 横レールの左右中心、縦レールの上下中心、ドラッグ元除外、向き別マーカー、空レール、端パンを実際の編集経路へ統合した。
  - 同一位置へのdropではドラフト、履歴、未保存表示、4レール、ピッカーを変更しないようにした。
  - クリック配置、精密drop、並べ替え、中央ゴミ箱、Undo・Redoを共通の配置ドラフト再描画経路へ統一した。
  - 編集バーの削除を既存の名前付きレイアウト削除コーディネーターへ接続し、既存のactive復元規則と2文書保存を再利用するようにした。
  - 削除成功時は復元されたactiveレイアウトで通常レイアウトUIを更新し、未保存確認を挟まず編集モードを終了して通常レールを再構築するようにした。
  - 削除失敗時は既存コーディネーターの候補を維持し、編集バーから同じ削除候補を明示再試行できるようにした。
  - 削除中は配置、履歴、保存、再削除、終了を停止するようにした。
  - 全129テストファイルの成功を確認した。
- DB-12H-1「削除失敗候補の再試行導線引き渡し」を実装した。
  - レイアウト削除の保存失敗後に編集を終了すると、管理コーディネーターの保留候補だけが残って再試行導線を失う完了監査上の問題を修正した。
  - 削除失敗候補を保持したまま編集終了した場合、レイアウト管理ダイアログの既存「保存を再試行」へ導線を引き渡すようにした。
  - 管理側の再試行では同じ削除候補を保存し、成功時に既存のactive復元、通常選択肢、編集可否、4レール再構築を同期するようにした。
  - 配置保存失敗候補は配置編集セッションの破棄とともに破棄されるため、管理側へ誤って引き渡さないようにした。
  - 全130テストファイルの成功と、Firefox拡張lintのエラー、警告、通知0件を再確認した。
- DB-12I「全体回帰・Firefox拡張lint」を実施した。
  - プロダクションビルドを含む全129テストファイルの成功を確認した。
  - Firefox拡張lintのエラー、警告、通知0件を確認した。
  - `git diff --check`で空白エラーがないことを確認した。
  - web-extの更新確認だけがローカル更新設定へのアクセス権不足を通知したが、拡張の検証結果には影響しないことを確認した。
- DB-12J「完了条件確認とDB-13引き渡し」を実施した。
  - 横・縦の中心境界、ドラッグ元除外、任意位置配置、同一・別レール並べ替えを配置ドラフトと精密dropテストへ対応付けた。
  - 恒久デフォルトを含む中央ゴミ箱未配置化、定義・チップ設定維持、ドラッグ中だけの表示と全終了経路での非表示を未配置化・ゴミ箱テストへ対応付けた。
  - 1操作1履歴、無変化除外、Undo・Redo後の全表示同期、保存基準まで戻った未保存解除を履歴・編集バー・パネル統合テストへ対応付けた。
  - メインレイアウト文書だけの保存、成功時基準化、編集継続、失敗候補維持、同一候補再試行、保存中ガードを配置編集保存セッションへ対応付けた。
  - 保存失敗候補とドラフトの分岐防止、および削除失敗後に編集終了した場合の管理側再試行導線を追加監査テストへ対応付けた。
  - 未保存終了確認、変更破棄、名前付きレイアウト削除とactive復元、内部デフォルト編集禁止を編集モード・既存削除処理との統合へ対応付けた。
  - キーボード配置・並べ替えを追加していないことを確認した。
  - チップの適用順、後方優先、共有状態の再合成、保存データ再読込後のメイン再構築をDB-13の対象として引き渡した。
  - 全130テストファイルの成功と、Firefox拡張lintのエラー、警告、通知0件を最終確認した。
- DB-12「レイアウト編集D&D」全体を完了した。
  - ユーザー完了確認済み。

## DB-12確定仕様・実装計画（完了）

### DB-12: レイアウト編集D&D

レール内の精密な挿入・並べ替え、ドラッグ中だけ表示する中央ゴミ箱への未配置化、編集履歴のUndo・Redo、保存と失敗復旧へ進む。仕様上の懸念事項は1問1答ですべて解消済みとし、新たな例外・矛盾・実装不能事項が判明しない限り再質問しない。

#### 確定仕様

- 横レールは左右中心、縦レールは上下中心を挿入境界にする。
- ドラッグ元を除外した並びへ挿入する。
- 中央ゴミ箱では恒久デフォルトベイを含む全ベイを未配置化できる。
- 未配置化してもベイ定義とチップ設定は削除しない。
- 中央ゴミ箱はベイドラッグ中だけ表示する。
- `dragend`、Escape、drop完了でゴミ箱を非表示にする。
- クリック配置、レールdrop、並べ替え、未配置化を各1履歴とする。
- ドラッグ中プレビューと端パンは履歴に含めない。
- 実質的に変化しないdropは履歴と未保存状態を追加しない。
- Undo・Redo後はドラフト、4レール、ピッカー、未保存状態を同期する。
- 保存済み状態までUndoした場合は未保存表示を解除する。
- 保存成功時は履歴を空にして新しい基準点にする。
- 保存成功後も編集モードを継続する。
- 保存対象はactiveレイアウトの配置を含むメインレイアウト文書だけとする。
- 保存失敗時はドラフト、履歴、未保存状態を維持する。
- 同じ保存候補を明示操作で再試行できる。
- 未保存で編集終了する場合は「編集を続ける／変更を破棄」を表示する。
- 保存中は編集、終了、再保存を停止する。
- 編集バーへUndo、Redo、保存、削除を追加する。
- レイアウト削除は既存の名前付きレイアウト削除処理と復元規則を再利用する。
- 内部デフォルトでは編集を禁止する。
- キーボードによる配置・並べ替えはスコープ外とする。

#### TDD実装フェーズ

| フェーズ | 内容 | コスト |
| --- | --- | --- |
| DB-12A | 配置ドラフトの任意位置挿入・並べ替え | M |
| DB-12B | Undo・Redo履歴と未保存判定 | L |
| DB-12C | 精密なレール挿入境界とdrop表示 | L |
| DB-12D | 中央ゴミ箱表示と未配置化 | M |
| DB-12E | 編集バーのUndo・Redo・保存・削除UI | M |
| DB-12F | 保存セッション、成功時基準化、失敗再試行 | L |
| DB-12G | 未保存終了確認と破棄経路 | M |
| DB-12H | パネル統合・既存粗drop置換 | L |
| DB-12I | 全体回帰・Firefox拡張lint | M |
| DB-12J | 完了条件確認とDB-13引き渡し | S |

各フェーズを `GO確認 → Red/Green → 全体回帰 → progress.md更新 → コミット文案 → STAY` の単位で進め、DB-12A〜DB-12Jと完了監査で追加したDB-12F-1、DB-12H-1を完了した。

## 次に着手する作業

### DB-13: 適用順とメイン再構築

上、左、右、下のレール順、各レール内のベイ順、各ベイ内のチップ順に条件チップを適用し、同じ状態キーは後方優先、異なる状態キーは合成する。操作チップの共有状態、アクションチップの明示実行、保存データ再読込後の状態リセットとメインUI再構築を一貫させる。

懸念事項の確認、仕様確定、テスト観点整理、実装フェーズ分割は完了済み。新規チャット向けの引き継ぎを含む詳細は `docs/DB-13plan.md` を参照する。

- DB-13A「activeレイアウトからチップ適用順を構築」を実装した。
  - activeレイアウトから上、左、右、下のレール順、各レール内のベイ順、各ベイ内のチップ順でruntime向けの単一適用列を構築するようにした。
  - 未配置ベイを適用列から除外し、空レイアウトでは空の適用列を返すようにした。
  - チップ設定を防御的にコピーし、入力文書の並びと設定を変更しないようにした。
  - 全131テストファイルの成功を確認した。

| フェーズ | 内容 | コスト |
| --- | --- | --- |
| DB-13A | activeレイアウトからチップ適用順を構築 | M |
| DB-13B | conditionの原子的評価と失敗継続 | L |
| DB-13C | Docking共有状態スキーマと標準キー検証 | L |
| DB-13D | 基本6 controlチップの定義と共有状態同期 | L |
| DB-13E | 汎用control/action実行境界 | M |
| DB-13F | 保存後の再読込・再評価セッション | L |
| DB-13G | 編集中プレビューと編集終了後のruntime再接続 | L |
| DB-13H | パネル統合・状態リセット・例外分離 | L |
| DB-13I | 全体回帰・Firefox拡張lint | M |
| DB-13J | 完了条件確認とDB-14引き渡し | S |

現在は **DB-13BのGO待ち**。DB-13Aまで実装済み。

## 保留・TODO

- エラー、警告、成功通知を共通の入口で扱う統合通知機能を設計する。詳細は `docs/todo.md` を参照する。
