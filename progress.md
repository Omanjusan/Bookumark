# 進捗

更新日: 2026-08-01

## ファイルの位置づけ

- `progress.md`: DB-14以降で更新する進捗の正本。
- `docs/progress-through-db13.md`: DB-13までの履歴を保持する読み取り用アーカイブ。
- `docs/progress.md`: DB-1着手前に作成された旧資料。履歴保持のため残すが、進捗の正本として扱わない。

## 完了済み

- DB-13「適用順とメイン再構築」まで完了済み。
- DB-13A〜DB-13Jの実装、回帰、完了監査を完了し、ユーザー完了確認済み。

## 最終検証結果

- 全137テストファイル成功。
- Firefox拡張lint: errors 0 / warnings 0 / notices 0。
- `git diff --check`: 問題なし。

## DB-14への引き渡し事項

- unknown/deprecatedチップの最終復旧UX。
- condition失敗の最終通知UI。
- 具体的な追加condition/action型。
- キーボードによる配置・並べ替えは対象外。

## 保留・TODO

- エラー、警告、成功通知を共通の入口で扱う統合通知機能はDB-14で設計済み、実装待ち。元要求は `docs/todo.md`、確定計画は `docs/db-14plan.md` を参照する。

## 再開位置

### DB-14: 廃止・破損復旧

DB-14の要求分析、懸念事項のインタビュー、仕様確定、テストケース設計、TDD実装フェーズ分割まで完了した。共通ダイアログと共通トーストをDB-14で作り、unknown/deprecated復旧とcondition失敗通知へ接続する。

- DB-14A Red「共通通知モデルとキュー」のテストを追加した。
  - ダイアログの単一active、FIFO、処理中ガードをテスト化した。
  - トーストの集約、発生回数更新、警告の8秒再計時、エラーの手動終了をテスト化した。
  - 不正契約の拒否、防御的スナップショット、存在しない通知への無変化応答をテスト化した。
  - プロダクションビルド成功後、未実装の`common-notification-queue.js`が存在しないことを理由に対象テストが失敗し、Red成立を確認した。
- DB-14A Green「共通通知モデルとキュー」を実装した。
  - ダイアログを単一activeとFIFO待機列で管理し、処理中の完了・重複操作を停止するようにした。
  - トーストを集約キーで更新し、発生回数と最新内容を維持するようにした。
  - warningを8秒後に自動終了し、同一通知の更新時に再計時するようにした。
  - errorを自動終了せず、手動終了できるようにした。
  - 通知契約を検証し、入力と返却スナップショットを防御的に分離した。
  - 全138テストファイルの成功を確認した。
- DB-14B「共通ダイアログ・トーストUI」をRed-Greenで実装した。
  - 見出しと説明を関連付けたネイティブ共通ダイアログを追加した。
  - politeなlive regionを持つ共通トースト領域を追加した。
  - active通知の内容、重要度、処理中状態、主操作をダイアログへ同期するようにした。
  - Escapeによる復旧フローの迂回を防ぎ、開始前要素へのフォーカス復帰を追加した。
  - トーストの順序、重要度、発生回数、手動終了を描画するようにした。
  - 狭いパネルと長文でも操作を維持するダイアログ・トーストCSSを追加した。
  - 入力スナップショットを変更せず、再描画時に古いトーストDOMを残さないことを確認した。
  - 全140テストファイルの成功を確認した。
- DB-14C「現行台帳・廃止台帳・分類」をRed-Greenで実装した。
  - 基本6 controlの型、表示名、kindを現行チップ台帳へ集約した。
  - rendererの型一覧とベイ工場の表示名解決を現行台帳の正本へ接続した。
  - プロダクション廃止台帳を空で開始し、テストでは複数世代の廃止レコードを注入できるようにした。
  - 現行、廃止、未知をバージョン比較に依存せず分類するようにした。
  - 全ベイのチップをベイID、ベイ名、instance ID付きで保存順に分類するようにした。
  - 現行・廃止の重複、両台帳の重なり、不正レコードを台帳生成時に拒否するようにした。
  - 分類結果と台帳索引を入力から防御的に分離した。
  - 全141テストファイルの成功を確認した。
- DB-14D「unknown部分復旧候補」をRed-Greenで実装した。
  - 現行、廃止、unknownが混在する文書からunknownだけを除外するようにした。
  - 残るチップの相対順を維持し、各ベイ内のorderを1始まりで再採番するようにした。
  - 全チップ除外後も空ベイ、レイアウト配置、メタデータを維持するようにした。
  - ベイ・チップ採番カウンターと正常チップのID・settingsを維持するようにした。
  - 除外対象をベイID、ベイ名、instance ID、chipType付きで保存順に返すようにした。
  - unknownがない場合は保存不要の無変更結果を返し、復旧済み候補への再適用を冪等にした。
  - 入力文書と返却候補・除外情報を防御的に分離した。
  - 全142テストファイルの成功を確認した。
- DB-14E「unknown／構造復旧保存セッション」をRed-Greenで実装した。
  - 既存の構造正常化結果とunknown除外候補を1つの保存候補へ統合した。
  - 変更文書をベイ、レイアウト、メタデータの固定順で和集合化し、1回のstorage要求で保存するようにした。
  - 復旧保存前・保存中・保存失敗時はruntime開始ゲートを閉じるようにした。
  - 復旧不要時はstorage要求を発生させず、初期状態からruntime開始可能にした。
  - 保存失敗後も候補と復旧情報を維持し、防御的コピーから同一候補を明示再試行するようにした。
  - 保存中の多重要求を拒否し、成功後の再実行では追加保存しないようにした。
  - 文書別recovery、変更文書、除外unknownを防御的スナップショットで返すようにした。
  - 全143テストファイルの成功を確認した。
- DB-14F「deprecated確認・削除セッション」をRed-Greenで実装した。
  - deprecatedインスタンスを型別・ベイ別に集約し、表示名、件数、廃止時期を通知用に返すようにした。
  - replacementChipTypeを現行台帳の表示名付き代替候補へ解決するようにした。
  - 確認前・保存中・保存失敗時はruntime開始ゲートを閉じるようにした。
  - 確認時に全ベイから全deprecatedを削除し、残るチップを連続orderへ再採番するようにした。
  - 空ベイ、配置、メタデータ、採番カウンター、正常チップ設定を維持するようにした。
  - 保存失敗後も集約情報と同一削除候補を維持し、防御的コピーから再試行するようにした。
  - 削除保存成功を確認済みの正本とし、保存済み文書の再検査で通知を繰り返さないようにした。
  - 全144テストファイルの成功を確認した。

- DB-14G「condition失敗通知アダプター」を完了した。
- condition適用失敗を1件のwarningトーストへ集約し、ベイ名・表示名・件数だけを利用者向け文面へ表示するアダプターを追加した。
- instanceId、chipType、reason、bay情報を診断情報として保持し、内部IDや失敗理由を通知本文へ露出しないことを確認した。
- 台帳や適用情報が欠ける場合は安全な表示へフォールバックし、入力を変更しないことを確認した。
- 全145テストファイルの成功を確認した。

- DB-14H「パネル起動・runtime統合」を完了した。
- 初回ロードの正常化を自動保存から分離し、unknown／構造復旧、deprecated確認・削除、通常runtime開始を固定順序で調停する起動オーケストレーターを追加した。
- 保存成功までruntime開始ゲートを閉じ、失敗時は共通ダイアログから同一候補を再試行できるようにした。
- 復旧不要時はダイアログと保存要求を増やさず、最終保存済み文書からruntimeを1回だけ開始することを確認した。
- 共通通知キュー／ビューをパネルへ接続し、condition失敗を診断ログと集約トーストへ通知した。警告トーストの自動終了も画面へ同期する。
- 全146テストファイルの成功を確認した。

- DB-14I「全体回帰・Firefox拡張lint」を完了した。
- 全146テストファイルの成功を確認した。
- Firefox拡張lintはerrors 0／warnings 0／notices 0を確認した。
- `git diff --check`に問題がないことを確認した。
- `web-ext`の更新確認はローカル設定ストアの権限案内を出したが、lint検証結果には影響していない。

- DB-14J「完了条件確認とDB-15引き渡し」の監査を実施した。
- DB-14A〜Iの機能、全146テスト、Firefox拡張lint、差分検査は成功済みである。
- 最終パネル表示には、unknownのベイ名・型・件数、構造復旧内容、deprecatedのベイ名・廃止時期・代替候補、保存失敗後の再試行表示が反映されていないことを確認した。
- 完了条件を満たさないためDB-14は完了扱いにせず、DB-15への正式引き渡しも保留する。
- 是正フェーズDB-14K「復旧ダイアログ最終表示」（コストM）を追加した。詳細は `docs/db-14plan.md` のDB-14J完了監査を参照する。

- DB-14K「復旧ダイアログ最終表示」をRed-Greenで完了した。
- unknownをベイ名・`chipType`・件数単位で集約し、構造補正と内部デフォルト復元を利用者向け名称で表示するようにした。
- deprecatedのベイ別件数、廃止時期、任意の代替候補を表示するようにした。
- 保存失敗後もactiveダイアログとキュー順を維持し、説明と主操作を「保存を再試行」へ更新するようにした。
- instance IDや例外詳細を画面へ出さず、入力スナップショットを変更しないことを確認した。
- 全147テストファイルの成功、Firefox拡張lintのerrors 0／warnings 0／notices 0、`git diff --check`の成功を確認した。
- DB-14Jで検出した未達5項目はすべて是正済みで、DB-14の技術的完了条件を満たした。

- 2026-07-29にユーザー最終確認を受け、DB-14「廃止・破損復旧」を完了した。

## DB-15への引き渡し

- Firefox実機での長文通知、ズーム、フォーカス、性能確認。
- 具体的な追加condition/action型。
- 既存のロード失敗・保存失敗などを共通通知へ全面移行する作業。
- 通知履歴。
- deprecatedチップから代替チップへの設定変換・自動置換。

## DB-15「Firefox実機・性能確認」計画

- 要求分析と全8問の懸念事項インタビューを完了した。
- DB-15をFirefox実機・性能確認と、発見した必須問題の必要最小限の是正に限定した。
- 正式対象環境をLinux版Firefox Flatpak、`develop`プロファイルに確定した。
- 高負荷fixtureを2,000ブックマーク、20ベイ、各10チップ、4レール使用に確定した。
- 5ズーム倍率、3画面条件、4系統のD&D、長文通知、フォーカス、性能測定の対象と合格方針を確定した。
- 追加condition/action型、既存通知経路の全面移行、通知履歴、deprecated自動置換はDB-16以降へ分離した。
- DB-15A〜DB-15Jの機能別フェーズとコストを`docs/db-15plan.md`へ記録した。
- DB-15の実装・実機検証には未着手。

次はDB-15A「実機fixtureと記録様式」のテストケース確認とGO確認から再開する。

- DB-15A「実機fixtureと記録様式」の自動化部分をRed-Greenで実装した。
  - 20ベイ、各10チップ、合計200チップを4レールへ決定的に配置するfixture生成を追加した。
  - productionの正常化を無変更で通過し、呼び出し間で状態を共有しないことをテストした。
  - 開発ビルド専用fixtureページを追加し、投入前のドッキング設定退避、2,000ブックマーク生成、fixture専用フォルダ削除、設定復元の導線を用意した。
  - production buildからfixtureページと実行コードを除外した。
  - `docs/db-15-results.md`へ実機結果と性能測定の記録様式を追加した。

次はDB-15AのFirefox実機投入・復元確認から続行する。

- DB-15A実機確認で、2,000件を作成したfixtureフォルダがBookumarkの現在フォルダへ選択されず、表示上の所在を判断できない必須問題を検出した。
- DB15-A-01として、`currentFolder`を退避・復元対象へ追加し、投入完了時にfixtureフォルダを現在フォルダへ設定するRed-Green是正を行った。
- DB-15Dの調査用に、4レールへ識別可能なベイを1つずつ配置するベイ編集調査fixtureと、高負荷fixtureへ戻す切替導線を追加した。
- DB-15D実機確認で、ベイ工場の対象選択アウトラインが表示されないDB15-D-01を検出した。
- ベイ工場の選択ID通知を4レールの実DOMへ接続し、対象ベイだけを3pxアウトラインで表示するRed-Green是正を行った。
- DB-15D実機画像からチップツール欄が空であることを確認し、空台帳と空D&D callbackが残るDB15-D-02を検出した。
- 現行6チップ台帳をツールセレクターへ接続し、追加・並べ替え・領域外削除をベイ編集セッションへ反映するようにした。
- ベイ名、Undo・Redo、保存状態を編集セッションへ接続し、保存後にレイアウト管理状態と通常runtimeを同じ文書から再構築するようにした。
- DB-15Dのベイ選択アウトライン、チップツール、追加、並べ替え、Undo・Redo、保存、再読込後の維持についてFirefox実機合格を確認した。
- DB-15D実機確認で、ベイ内部のチップ方向をレール内の複数ベイ配置へ誤用していたDB15-D-03を検出した。
- 上下レールではベイを垂直、左右レールでは水平に配置し、上・左・右・下の全レールで保存順が外側から内側へ増えるよう、逆方向レールのD&D座標と挿入マーカーを含めてRed-Green是正した。
- DB-15D実機確認で、新規ベイが編集トランザクションへ接続されずチップを置けないDB15-D-04を検出した。
- 新規ベイを一時ID付きの通常編集セッションへ接続し、チップ追加・並べ替え・Undo・Redo後の保存で正式ベイIDへ置換するようRed-Green是正した。
- DB-15D実機確認で、新規保存後の名前変更が直前ベイの改名になり、配置先も上固定であるDB15-D-05を検出した。
- 新規作成は「保存して閉じる」で成功時に終了し、次回追加では別IDを発行するようにした。4方向の配置先選択、各方向の内側末尾配置、同名拒否時のドラフト保持をRed-Greenで追加した。
- DB15-D-03〜D-05のレール配置、新規ベイ編集、保存終了、4方向配置、別名追加、同名拒否についてFirefox実機合格を確認した。
- DB-15E端パン確認で、レールにスクロール軸は設定されているが表示領域の上限がなく、内容に合わせて拡大するためスクロールバーと端パン条件が成立しないDB15-E-01を確認した。
- 端パンの目的は、レールからあふれた見えない挿入位置へD&D中に到達させることである。レール上限寸法と画面占有率の仕様が未確定のため、現状維持のうえペンディングとし、後続で仕様策定・実装・実機確認する。
- DB-15Eは端パン以外のブックマークD&D確認へ継続する。
- DB-15Eのカスタム配置D&Dについて、挿入位置、再読込後の独自順序維持、Firefox本体の順序非変更、キャンセル時の表示解除をFirefox実機で確認した。
- DB-15Eの公式整理・同階層D&Dについて、Firefox本体への反映、成功通知、Undo、再読込後の維持、処理中ガードをFirefox実機で確認した。
- DB-15Eの公式整理・別フォルダD&Dについて、fixture内フォルダへの移動、Firefox本体との一致、Undoによる復帰をFirefox実機で確認した。
- DB-15Fの長文通知・フォーカス検証用に、20ベイ・200種類の未知チップを安全な復旧対象として投入する開発fixtureを追加した。復旧後は高負荷fixtureへ戻せる。
- 20空ベイが200%・狭幅で通常UIを操作不能にすることをDB15-E-01の波及証跡として記録した。DB-15Fの検証責務を分離するため、長文量は未知チップ200種類のまま上レール1ベイへ集約し、復旧後に残る空ベイを1個へ限定した。
- DB-15Fの長文復旧ダイアログについて、200%・狭幅で主操作フォーカス、本文スクロール、フォーカス閉じ込め、内部詳細の非表示、復旧後操作をFirefox実機で確認した。
- トーストのフォーカス非奪取は自動DOMテストで維持されている。実機発火に必要な具体的condition型はDB-15対象外のため、実機確認を後続へ引き渡した。
- DB-15Gの性能初回測定へ移行した。
- Node補助ベンチマークで2,000件の描画計画を100回測定し、中央値0.913ms、p95 1.376ms、最大1.790msを記録した。
- Firefox実機の7操作を同じ区間で5回ずつ測るため、開発ビルドだけに二重`requestAnimationFrame`基準の計測パネルを追加した。試行値は再読込をまたいで保持し、JSONコピーできる。
- Firefox性能初回測定の途中値を取得した。起動5回は中央値355ms・最大362ms、表示形式切替9回は中央値107ms・最大147ms、検索2回は中央値104ms・最大105ms、レイアウト切替7回は中央値105ms・最大111ms、D&D再描画7回は中央値27ms・最大88msだった。
- 検索をあと3回、ソートとレイアウト編集開始を各5回追加測定してから合格基準案を確定する。
- Firefox性能初回測定の全7項目を取得した。起動は中央値355ms・最大362ms、対話操作は中央値最大107ms・単発最大147msで、操作不能、著しい待ち時間、未処理停止はなかった。
- 合格基準案を、起動は中央値500ms以下かつ最大750ms以下、6つの対話操作は各中央値150ms以下かつ最大250ms以下とした。ユーザー合意後にDB-15Hの確定基準とする。
- ユーザー合意によりDB-15Hの性能基準を確定した。起動は中央値500ms以下・最大750ms以下、対話操作6項目は各中央値150ms以下・最大250ms以下とし、操作不能・データ欠落・未処理停止は時間にかかわらず不合格とする。
- DB-15Hの最終性能測定を完了した。起動は中央値350ms・最大409ms、対話操作は中央値最大105ms・単発最大144msで全基準を満たした。
- 最終JSONで未採取だったレイアウト切替は、対象実装変更を挟んでいない直前の同条件測定（中央値105ms・最大111ms）を採用した。
- 性能測定中に操作不能、データ欠落、未処理停止は確認されず、DB-15Hの性能条件を合格とした。
- DB-15Iの全条件再検証・回帰へ移行する。
- DB-15Iの表示再検証で、ベイ編集調査fixtureと2,000ブックマークを用い、5ズーム倍率と3画面条件の全15組み合わせをFirefox実機で確認した。
- 全条件でヘッダー到達、ブックマーク表示、ページ全体の横スクロール非発生、主要チップ操作、文字・ボタンの非崩壊を確認した。DB15-E-01の20ベイ無制限展開は既知のPENDINGとして判定から除外した。
- DB-15Iの全体回帰で全153テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-15Aのfixture復元実機確認で、二重投入拒否、元設定・現在フォルダの復元、fixture専用フォルダの削除、元ブックマークの維持を確認した。
- DB-15J完了監査を実施し、DB-15A〜Iの自動検証、Firefox実機確認、性能基準、fixture復元の証跡が揃っていることを確認した。
- DB-15は、ユーザー承認済みPENDINGのDB15-E-01（レール上限仕様と端パン実機検証）を後続へ引き渡す条件で完了監査PASSとした。
- 具体的condition型導入後のトースト・フォーカス実機確認も後続候補として維持する。
- DB15-E-01を独立フェーズとして扱うことを確定し、次チャットで要求分析から再開できる引き渡し文書`docs/db15-e-01plan.md`を追加した。
- DB15-E-01は現在`PLANNING_NOT_STARTED`であり、次は同文書を入口に懸念事項と質問総数を提示してインタビューを開始する。実装GOは未取得である。
- DB15-E-01の要求分析と一問一答の懸念事項インタビューを完了した。
- 全体利用幅96%、上下各25%、左右各20%、中央原則270px、狭幅時の中央優先を確定した。利用幅の将来設定余地は文書だけに残し、設定機能は対象外とした。
- レールの片軸スクロール、巨大ベイ内部の別軸スクロール、リサイズ時の位置維持、レイアウト切替時の先頭化を確定した。
- レール端パンはマウスによるベイD&Dだけを対象とし、ブックマークD&Dへの接続は既存契約がないため対象外とした。
- TC-17を欠番とする確定31テストケースと、DB15-E-01A〜Gの機能別実装計画を`docs/db15-e-01plan.md`へ記録した。
- DB15-E-01Aの計画文書更新を完了した。確定値、対象範囲、対象外、31ケース、A〜Gのフェーズ分割を検査し、`git diff --check`の成功を確認した。
- 次はDB15-E-01B「全体幅・レール上限・中央優先」のGO確認から再開する。
- DB15-E-01B「全体幅・レール上限・中央優先」をRed-Greenで完了した。
- 全体利用幅96%を将来差し替え可能なCSS変数へ分離し、固定640px上限とbodyの360px最低幅を撤廃した。
- 左右レールを各20%上限、中央を縮小可能な原則270px、上下レールを各25vh上限とし、狭幅時は中央を優先できるGrid契約を追加した。
- 対象CSSテスト、全153テスト、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- Firefox実機での15表示条件はDB15-E-01Gで確認する。次はDB15-E-01C「レール片軸・巨大ベイ内部スクロール」のGO確認から再開する。
- DB15-E-01C「レール片軸・巨大ベイ内部スクロール」をRed-Greenで完了した。
- 上下レールの横長ベイは外枠をレール幅以内に制限し、超過内容だけをベイ内部で横スクロールするようにした。
- 左右レールの縦長ベイは外枠を100vh以内に制限し、超過内容だけをベイ内部で縦スクロールするようにした。
- レール側の上下縦・左右横の片軸スクロールと、8px・4px・2pxの既存間隔契約を維持した。
- 対象CSS・描画テスト、全153テスト、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 入れ子スクロールのFirefox実機操作感はDB15-E-01Gで確認する。次はDB15-E-01D「リサイズ位置補正・切替先頭化」のGO確認から再開する。
- DB15-E-01D「リサイズ位置補正・切替先頭化」をRed-Greenで完了した。
- リサイズ通知時に4レールの間隔・overflowを再評価し、現在位置を新しい有効範囲内へ補正するようにした。
- 通常方向の正値と、`row-reverse`・`column-reverse`で生じる負値の両方を符号を保って補正する純粋計算を追加した。
- 同じレイアウトの再描画ではスクロール位置を維持し、初回構築またはactiveレイアウトID変更時だけ各レールを外側の先頭へ戻すようにした。
- 対象位置計算・レイアウトライフサイクルテスト、全154テスト、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- リサイズとレイアウト切替のFirefox実機確認はDB15-E-01Gで行う。次はDB15-E-01E「連続端パン・停止」のGO確認から再開する。
- DB15-E-01E「連続端パン・停止」をRed-Greenで完了した。
- ベイD&Dの最新レール・ポインター位置を単一の`requestAnimationFrame`ループで保持し、ポインター静止中も端パンを継続するようにした。
- 端から離れた場合、dragleave、clear、drop、disconnect、ドラッグ状態消失、レール終端で実移動できない場合にループを停止する。
- 新しいdragoverが続いてもフレームを多重予約せず、端パン速度と上下縦・左右横の片軸契約を維持した。
- Nodeテスト環境では短いタイマーへフォールバックし、Firefoxでは`requestAnimationFrame`を使用する。
- 対象D&D・端パンテスト、全154テスト、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 静止中の連続端パンと全終了経路のFirefox実機確認はDB15-E-01Gで行う。次はDB15-E-01F「保存・境界値・高負荷・回帰」のGO確認から再開する。
- DB15-E-01F「保存・境界値・高負荷・回帰」を完了した。
- TC-26は配置編集保存、TC-27は20ベイ・4レールfixture、TC-29は上限ぴったりと超過、TC-30は位置補正とD&D停止、TC-31は既存永続化回帰で自動テスト済みであることを監査した。
- 対応する集中7テストはすべて合格し、保存形式、高負荷fixture、境界値、停止、既存永続化契約に未対応はなかったため、新しい実装やテスト追加は不要と判断した。
- 全154テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次はDB15-E-01G「Firefox実機15表示条件・最終監査」のGO確認から再開する。
- DB15-E-01Gの端パン実機確認開始時、ベイ編集調査fixtureの上部ベイが固定ベイだけで配置編集D&Dの対象に出ないことを確認した。
- 調査fixtureへ非固定の「上レール・D&D確認」ベイを追加し、上部レールの2番目へ決定的に配置するRed-Green是正を行った。
- 全155テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次は更新した開発ビルドで調査fixtureを再投入し、TC-14から端パン実機確認を再開する。
- DB15-E-01GのFirefox実機確認でTC-14はPASSしたが、TC-15はマウスポインターを動かした時だけ端パンし、静止中の連続動作と端へ近いほど速くなる契約が成立せずFAILとなった。
- Firefoxでは`scrollBy()`の反映が次フレームになるため、呼出直後の位置比較で連続ループを終了していたことを遅延反映テストでRedとして再現した。
- 前回観測位置との比較を次フレームへ持ち越し、2フレーム連続で位置が変わらない場合だけレール終端として停止するRed-Green是正を行った。
- 対象D&D・端パンテスト、全155テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次は更新した開発ビルドでTC-15をFirefox実機再確認する。
- TC-15再確認中、レイアウト編集中も中央の2,000ブックマーク表示がページ高さを維持し、上下レール間の移動へ不要な長距離スクロールを要求する仕様問題を確認した。
- 編集中はフォルダ一覧とブックマーク一覧を含む中央通常コンテンツを完全に隠し、ベイD&D中の中央ゴミ箱だけを維持する仕様へ変更した。
- 編集開始前のページスクロール位置をセッション単位で退避し、通常終了、保存終了、変更破棄、削除終了の共通終了経路で通常runtime再接続後に復元する。
- 対象レイアウト・編集ライフサイクルテスト、全155テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次はFirefox実機で編集時の中央空欄化、上下レール間距離、終了後の通常表示・ページ位置復元を確認してからTC-15を再開する。
- DB-15 fixtureページの操作を、投入・復元、fixtureメニュー、アドオン起動の3グループへ再編成し、グループ間へスプリッターを追加した。
- 「fixtureをBookumarkで開く」は、既存の起動処理とボタンIDを維持したまま「アドオン実行」へ名称変更した。
- fixtureページ集中テスト、全155テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次は更新したfixtureページの表示をFirefox実機で確認し、中央空欄化の実機確認へ戻る。
- fixtureページ再編成、レイアウト編集中の中央通常コンテンツ空欄化と上下レール間距離短縮、編集終了後の通常表示・ページ位置復元をFirefox実機で確認しPASSとした。
- 中央ゴミ箱はベイ定義を削除せず、対象を「未配置」グループへ移す既存契約どおりであることをFirefox実機で確認した。
- TC-15は再確認後も、ポインター移動中だけ下方向へ動き、静止中の連続端パンと端へ近いほど速くなる速度差が成立しないためFAILとした。
- レイアウト編集は他のコア機能を確認する足場として現在の実装を維持し、仕様全体の再設計判断まで`DB15-E-01G_FROZEN_WITH_KNOWN_LIMITATIONS`として凍結することをユーザーと合意した。
- TC-14はPASS、TC-15はFAIL、TC-16およびTC-18〜23は未確認であり、DB15-E-01Gを完了またはオールグリーンとは扱わない。
- 次はレイアウト編集に依存しないDB-16「既存エラー経路の共通通知への移行」の要求分析へ進む。
- DB-16「既存エラー経路の共通通知移行・公式整理凍結」の要求分析と全6問の懸念事項インタビューを完了した。
- 初期読み込み失敗は再試行可能な共通ダイアログ、フォルダ・履歴移動失敗と仮想カスタム順序保存失敗は共通エラートーストへ移行する仕様を確定した。
- 初期読み込みだけ明示再試行と多重実行防止を持ち、その他は状態を維持して利用者の再操作または次回全量保存を待つ。
- 公式整理は過剰な正本変更・復旧責務を避けるため正式機能から外し、UIから非表示、過去状態を`custom-order`へ正規化し、実装コードとテストは凍結保存する方針を確定した。
- 開発ビルド限定の一回限りの初期読み込み失敗・仮想順序保存失敗fixtureを用意し、production除外とFirefox実機確認を行う。
- 確定したTC-01〜30、DB-16A〜Hの機能別計画、コスト、懸念事項、完了条件、次チャット再開文面を`docs/db-16plan.md`へ記録した。
- DB-16A「確定仕様、テストケース、計画文書」を完了し、状態を`DB-16A_COMPLETE`とした。DB-16B〜Hの実装GOは未取得であり、次はDB-16Bのテストケース確認とRed-Green GO確認から再開する。
- DB-16B「共通通知アダプターと固定文面・診断」のRed-Greenを完了し、状態を`DB-16B_COMPLETE`とした。
- 初期読み込み、フォルダ・履歴移動、ブックマーク／フォルダ仮想順序保存の4操作を、内部情報を含まない固定文面と操作別診断へ分離した。
- 同種トーストは操作別の固定`aggregateKey`と一意な通知IDを使い、既存キューで発生回数を集約する。ブックマーク順序保存とフォルダ順序保存は同じ利用者文面を使いながら診断と集約キーを区別する。
- 診断、通知登録、描画、その失敗報告が例外を投げても呼び出し元へ逆流しないアダプター境界を追加した。初期読み込み再試行、移動状態復元、楽観表示と次回全量保存の経路統合はDB-16C〜Eへ残した。
- DB-16B対象6テスト、全156テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-16C〜Hの実装GOは未取得であり、次はDB-16C「初期読み込み失敗ダイアログと再試行」のテストケース確認とRed-Green GO確認から再開する。
- DB-16C「初期読み込み失敗ダイアログと再試行」のRed-Greenを完了し、状態を`DB-16C_COMPLETE`とした。
- 初期読み込みを全量`load → publish`境界として制御し、成功時だけ通常状態を1回公開する。失敗時は内部詳細を含まない共通エラーダイアログを表示し、中央の専用エラー表示は使用しない。
- Docking、ブックマークツリー、仮想順序、現在フォルダ、履歴用データをローカル候補へ読み込み、全非同期処理が成功してからパネル状態とruntimeへまとめて公開する順序へ変更した。
- 「再試行」は全初期読み込みを最初から実行し、busy中の再試行と外部からの重複開始を拒否する。成功時はダイアログを完了し、再失敗時は同じダイアログを再試行可能状態へ戻す。
- activeダイアログと待機列のFIFOを維持し、初期失敗ダイアログが待機中の場合は主操作を開始しない。診断・描画失敗も再試行状態へ逆流させない。
- DB-16C対象7テストとパネル接続回帰、全157テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-16D〜Hの実装GOは未取得であり、次はDB-16D「フォルダ・履歴移動失敗トースト」のテストケース確認とRed-Green GO確認から再開する。
- DB-16D「フォルダ・履歴移動失敗トースト」のRed-Greenを完了し、状態を`DB-16D_COMPLETE`とした。
- 移動先のフォルダ一覧、ブックマーク履歴情報、現在フォルダ保存値をローカル候補として完成させ、すべて成功した場合だけ表示と現在フォルダをまとめて公開する境界を追加した。
- フォルダ訪問と戻る／進むは移動成功後だけ履歴を進め、失敗時は移動前表示、現在フォルダ、履歴位置を維持して固定文面の共通エラートーストを表示する。
- フォルダ名、内部ID、例外文は通知本文へ含めず操作別診断だけに保持する。同種失敗は固定`aggregateKey`で集約され、通知失敗と手動終了は復元済み状態へ影響しない。
- DB-16D対象4候補ロードテストと通知・パネル接続回帰、全158テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-16E〜Hの実装GOは未取得であり、次はDB-16E「仮想カスタム順序保存失敗トースト」のテストケース確認とRed-Green GO確認から再開する。
- DB-16E「仮想カスタム順序保存失敗トースト」のRed-Greenを完了し、状態を`DB-16E_COMPLETE`とした。
- ブックマークとフォルダのD&D後は画面上の楽観順序を先に維持し、仮想順序保存の成功後だけメモリ上の保存済み基準`folderOrders`を更新する順序へ変更した。
- 保存失敗時は同じ固定文面の共通エラートーストを表示し、ブックマーク／フォルダの操作種別を診断と固定`aggregateKey`で区別する。例外文や内部GUIDは画面へ表示しない。
- 保存失敗後もD&Dと通常操作を継続でき、次の並べ替えでは現在表示の最新全量GUID順を保存要求へ渡す。通知callback自身の失敗も保存処理へrejectとして逆流させない。
- `error`トーストは自動終了せず、同種失敗を既存キューで集約し、手動終了しても現在表示を変更しない既存契約を維持した。
- DB-16E対象の全量再保存・通知隔離テストと通知・パネル接続回帰、全158テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-16F〜Hの実装GOは未取得であり、次はDB-16F「公式整理UI無効化とruntime正規化」のテストケース確認とRed-Green GO確認から再開する。
- DB-16F「公式整理UI無効化とruntime正規化」のRed-Greenを完了し、状態を`DB-16F_COMPLETE`とした。
- 動的移動モードチップと旧入力bindingを「カスタム配置」「通常」の2択へ変更し、「公式整理」、Firefox本体反映の説明、専用選択スタイルをproduction UIから除外した。
- 保存互換性と凍結コードのため`directory-move`型は維持し、起動・再構築・control更新のruntime境界で`custom-order`へ非破壊正規化する関数を追加した。
- 正規化は入力共有状態を変更せず、Firefoxブックマーク、仮想順序、Docking保存文書へ書き戻さない。通常モードでは既存どおり並べ替えD&Dを許可しない。
- 公式移動、正本再取得、Undoの実装は変更・削除せず、既存の公式移動モジュールテストがすべて成功することを確認した。
- DB-16F対象の2択UI・runtime正規化テストと公式移動凍結回帰、全158テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-16G〜Hの実装GOは未取得であり、次はDB-16G「開発限定・一回限りの失敗fixture」のテストケース確認とRed-Green GO確認から再開する。
- DB-16G「開発限定・一回限りの失敗fixture」のRed-Greenを完了し、状態を`DB-16G_COMPLETE`とした。
- 開発fixtureページへ、次回の初期読み込み失敗、次回の仮想表示順保存失敗、DB-16失敗スイッチ解除の3操作を追加した。DB-15バックアップ復元とDB-16失敗スイッチは独立しており、復元操作では失敗準備を解除しない。
- 失敗準備は操作別booleanとして保持し、同じ失敗の二重準備でも回数を積み上げない。発火時は例外を投げる前にスイッチを解除し、初期読み込みと仮想順序保存をそれぞれ一回だけ失敗させる。
- development buildだけで失敗fixtureを`panel.js`より前に注入し、初期storage読み込みと`orderByFolder`を含むstorage保存を対象にした。production buildでは`dist/dev`、fixture UI、失敗スイッチ文字列、開発実行コードが存在しないことを確認した。
- DB-16G対象7テスト、失敗スイッチの実動作確認、全158テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DB-16Hの実装GOは未取得であり、次はDB-16H「Firefox実機、全体回帰、完了監査」のテストケース確認とGO確認から再開する。
- DB-16H「Firefox実機、全体回帰、完了監査」を完了し、DB-16全体の状態を`DB-16_COMPLETE`とした。
- TC-28は開発fixtureで次回初期読み込みを一回だけ失敗させ、共通ダイアログ、再試行中表示、多重実行防止、明示再試行後の正常復帰をFirefox実機で確認しPASSとした。
- TC-29は仮想表示順保存を一回だけ失敗させ、共通エラートースト、楽観表示順維持、同種通知集約、手動終了をFirefox実機で確認しPASSとした。
- TC-01〜27の自動契約とTC-28〜29の実機証跡を監査し、TC-01〜30がすべて成功してDB-16の完了条件を満たすことを確認した。DB15-E-01Gは既知制約付き凍結のままで再開していない。
- 最終回帰は全158テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、production除外検査、`git diff --check`がすべて成功した。実機終了後はproduction成果物へ再構築済みである。
- 上下2ベイ再設計TB-1「新保存モデル、初期値、不変条件」のRed-Greenを完了し、状態を`TB-1_COMPLETE`とした。
- 上下2ベイ専用のschema、最大3行の単一定義、上1行・下0行・上system・既定6チップの初期構成を追加した。
- systemベイ最低1行、表示行・所属行の範囲、instance IDと行内orderの一意性、採番境界を表明し、非表示行チップを保持する契約と防御的コピーを追加した。
- TB-1対象8テスト、全159テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-2以降の実装GOは未取得であり、次はTB-2「正規化、破損復旧、単一キー保存」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-2「正規化、破損復旧、単一キー保存」のRed-Greenを完了し、状態を`TB-2_COMPLETE`とした。
- `twoBayConfiguration.v1`だけを読み書きする独立ストレージ境界を追加し、旧3キーを読込・保存要求へ含めない契約を固定した。
- 新キー未保存と構造破損は内部初期値候補へ復旧し、部分破損は表示行数、system最低1行、chip ID、所属行、行内order、settings、採番境界だけを補正する。未知chip typeと非表示行チップは保持する。
- 正常構成は暗黙保存せず、補正・fallback候補だけを保存する。復旧保存失敗は呼び出し元へ伝播し、候補保存成功前に通常runtimeを開始させない境界を用意した。
- TB-2対象12テスト、全161テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-3以降の実装GOは未取得であり、次はTB-3「新起動境界と旧ランタイム切断」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-3「新起動境界と旧ランタイム切断」のRed-Greenを完了し、状態を`TB-3_COMPLETE`とした。
- 復旧保存済みの上下2ベイ構成だけを返すpanel bootstrapを追加し、active初期読み込みを新構成とブックマーク候補の原子的公開へ切り替えた。
- 旧3文書読込、旧復旧、旧保存、activeレイアウト評価、4レール再構築を通常起動から除外した。旧起動コードは削除せず、通常起動コントローラーから参照されない凍結関数として残した。
- 旧レイアウト選択、復元、管理、編集、ベイ追加・編集、左右レールを通常DOM表面から切り離し、active runtimeを`two-bay`として識別可能にした。
- TB-3対象5テスト、全163テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-4以降の実装GOは未取得であり、次はTB-4「上下ベイ通常表示と固定レイアウト」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-4「上下ベイ通常表示と固定レイアウト」のRed-Greenを完了し、状態を`TB-4_COMPLETE`とした。
- 新構成から表示行だけを外側から内側の描画計画へ変換し、各行を独立した横スクロール領域として描画する上下ベイviewを追加した。0行の非systemベイは通常画面で完全に非表示となる。
- 上ベイは下方向、下ベイは上方向へ行を積み、非表示行のチップを描画・実行対象から除外した。未知チップは保存状態を変更せず描画だけをスキップする。
- パネルを上・中央・下の全高3領域へ変更し、ページ全体を非スクロール、中央ブックマーク領域だけを縦スクロールにした。
- 基本6チップを新しい上下ベイから既存の日常操作runtimeへ接続した。
- TB-4対象7テスト、全166テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-5以降の実装GOは未取得であり、次はTB-5「`︙`、設定画面、system切り替え」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-5「`︙`、設定画面、system切り替え」のRed-Greenを完了し、状態を`TB-5_COMPLETE`とした。
- 左上固定の`︙`システムメニューと設定ダイアログを追加し、ベイ編集と初期化は後続フェーズまで無効な導線として配置した。
- systemベイ切り替えを新しい単一構成へ即時保存し、0行の切り替え先だけを1行へ展開した。旧systemベイの行数とチップ構成は変更しない。
- 保存失敗時は候補を保持し、ラジオ選択、閉じる操作、Escapeを無効化して、再試行またはキャンセルだけを許可した。キャンセル時は保存済み選択へ復元する。
- TB-5対象9テスト、全169テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-6以降の実装GOは未取得であり、次はTB-6「編集セッションと中央キャンバス」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-6「編集セッションと中央キャンバス」のRed-Greenを完了し、状態を`TB-6_COMPLETE`とした。
- 保存済み構成を共有参照のないbaselineとdraftへ複製する編集セッションを追加し、更新操作と外部参照から通常構成を分離した。
- `︙ → ベイ編集`を有効化し、中央の通常UIを隠して上下ベイを実位置に残す編集モードを追加した。
- 中央へ最低640×360 CSS pxの編集キャンバス、チップツールボックスと除去領域の配置先、確定・キャンセルの固定フッターを追加した。中央キャンバスだけをスクロール可能にした。
- キャンセルはdraftを破棄して通常画面へ戻す。確定保存はTB-11まで無効状態とした。
- TB-6対象7テスト、全172テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-7以降の実装GOは未取得であり、次はTB-7「行数操作と0行プレースホルダー」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-7「行数操作と0行プレースホルダー」のRed-Greenを完了し、状態を`TB-7_COMPLETE`とした。
- draftの表示行数だけを1段階変更するロジックを追加し、編集表示の各ベイ左側へ`aria-label`付きの`＋`と`－`を配置した。
- 3行の`＋`、0行の`－`、systemベイ1行の`－`を無効化し、非systemベイは0行まで縮小可能にした。
- 0行の非systemベイを非ドロップの1行相当プレースホルダーとして残し、「非表示設定が有効です」をオーバーレイ表示した。
- 行数変更では非表示行を含むチップ配列、所属行、順序、固有設定を変更しない。
- TB-7対象6テスト、全173テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-8以降の実装GOは未取得であり、次はTB-8「タブ式ツールボックスと追加D&D」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-8「タブ式ツールボックスと追加D&D」のRed-Greenを完了し、状態を`TB-8_COMPLETE`とした。
- 表示カテゴリとchip typeを分離した定義から「絞り込み」「ナビゲーション」「表示」「情報」の4タブを描画した。
- 既存6チップを可視行へcopy D&Dし、同型を含む各dropで独立IDを発行して対象行末尾へ追加するdraft操作を接続した。
- 0行プレースホルダー、非表示行、ベイ間の隙間、中央領域外へのdropは無効とした。
- 情報タブの日付・時計は定義と表示まで追加し、モック描画を追加するTB-10までD&Dを無効にした。
- TB-8対象7テスト、全177テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-9以降の実装GOは未取得であり、次はTB-9「行内・行間・上下間移動、除去D&D」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-9「行内・行間・上下間移動、除去D&D」のRed-Greenを完了し、状態を`TB-9_COMPLETE`とした。
- 編集中の配置済みチップだけをドラッグ可能にし、instance ID基準で同一行内、別行、上下ベイ間へ移動するdraft操作を追加した。
- drop位置を行内チップの中心境界から算出し、移動元と移動先のorderを1から再採番した。
- 中央の明示的な除去領域へのdropだけで対象instanceを削除し、元の行のorderを再採番した。
- 0行プレースホルダー、ベイ間の隙間、中央領域外へのdropとドラッグ中断は無効とし、ベイ自体にはドラッグ開始手段を設けなかった。
- TB-9対象9テスト、全179テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-10以降の実装GOは未取得であり、次はTB-10「日付・時計モックチップ」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-10「日付・時計モックチップ」のRed-Greenを完了し、状態を`TB-10_COMPLETE`とした。
- 情報タブの日付・時計を有効化し、既存チップと同じ追加・移動・除去D&Dへ接続した。
- 日付と時計を固定文言で描画する追加rendererを実装し、同型の複数instanceも独立DOMとして保持した。
- モックrendererは実日時取得、定期更新、書式設定、イベントlistener、共有状態のread/updateを行わない。
- 既存6チップのregistry契約を維持し、日付・時計は新しい上下2ベイruntimeにだけ追加登録した。
- TB-10対象6テスト、全180テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-11以降の実装GOは未取得であり、次はTB-11「確定、キャンセル、保存失敗再試行」のテストケース確認とRed-Green GO確認から再開する。
- 上下2ベイ再設計TB-11「確定、キャンセル、保存失敗再試行」のRed-Greenを完了し、状態を`TB-11_COMPLETE`とした。
- dirtyなdraft全体を確定時に1回だけ保存し、成功後だけ正式状態へ昇格して通常画面へ戻す編集トランザクションを追加した。
- 保存失敗時は同一候補を保持し、再試行では候補を変更せず再保存する。保存中と失敗候補保持中は行数操作、追加、移動、除去を遮断した。
- 保存失敗後のキャンセルでは候補とdraftを破棄し、baselineへ完全復元した。
- 編集保存成功後はsystem切り替えセッションの正本も同期し、次の操作が古い構成へ戻らないようにした。
- TB-11対象7テスト、全180テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- TB-12以降の実装GOは未取得であり、次はTB-12「初期化と確認・失敗復旧」のテストケース確認とRed-Green GO確認から再開する。
- UI改善 UI-1「タイトル情報チップの契約、初期配置、保存・復元」のRed-Greenを完了し、状態を`UI-1_COMPLETE`とした。
- 情報タブへ`bookmark-summary`（表示名「タイトル・件数」）を追加し、通常チップと同じ追加・移動・配置解除D&Dへ接続した。
- 初期構成の上ベイ先頭へタイトル情報チップを配置し、表示中のブックマーク件数へ追従して`Bookumark N件`を描画する情報runtimeを追加した。
- 上下2ベイschemaをv2へ更新し、既存v1構成はベイ、行、チップ、設定を維持したままタイトル情報チップを一度だけ先頭追加して保存する。v2で配置解除済みの場合は再追加しない。
- UI-1対象テスト、全183テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- UI-2以降の実装GOは未取得であり、次はUI-2「`︙`固定枠の上下連動と編集時表示」のテストケース確認とRed-Green GO確認から再開する。
- UI改善 UI-2「`︙`固定枠の上下連動と編集時表示」のRed-Greenを完了し、状態を`UI-2_COMPLETE`とした。
- `︙`を通常チップ配列とは独立した固定systemスロットで包み、選択中systemベイの1行目左端へ描画するようにした。固定枠はチップの追加・移動・除去対象に含めない。
- systemベイ切り替え後は同じ固定枠DOMを上段または下段へ移し、通常表示とベイ編集表示の両方で常時表示する。固定枠が占有する幅への特別な補正・警告は追加していない。
- メニュー本体は横スクロール領域外に維持し、固定枠の実座標から上段では下向き、下段では上向きにviewport基準で配置するようにした。
- UI-2対象テスト、全183テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- UI-3以降の実装GOは未取得であり、次はUI-3「旧タイトル段撤去と統合レイアウト」のテストケース確認とRed-Green GO確認から再開する。
- UI改善 UI-3「旧タイトル段撤去と統合レイアウト」のRed-Greenを完了し、状態を`UI-3_COMPLETE`とした。
- 上下2ベイruntime開始時に、旧タイトル、旧件数、旧レイアウト管理、旧ベイ工場導線を含むheader全体を非表示化し、通常UIでの専有高を0にした。
- 凍結した旧runtimeのDOM参照互換性を維持するためheader要素自体は削除せず、既存の旧surface切断境界へheaderを追加した。
- system固定枠を持つ1行目へ`two-bay-row--system`と「統合ツールバー」のアクセシブル名を付け、固定枠と情報・操作チップが同じ段であることを明示した。
- UI-3対象テスト、全183テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- UI-4以降の実装GOは未取得であり、次はUI-4「3面の視覚階層、余白、空ベイ表示」のテストケース確認とRed-Green GO確認から再開する。
- UI改善 UI-4「3面の視覚階層、余白、空ベイ表示」のRed-Greenを完了し、状態を`UI-4_COMPLETE`とした。
- ライト／ダークテーマへ中央専用の`--content-surface`を追加し、操作ベイの`--surface`より暗いブックマーク面として適用した。
- 上ベイ、中央、下ベイのgrid行間へ10pxの余白を設け、中央面へ上下境界線、角丸、内側余白を、通常ベイ面へ薄いshadowを追加した。
- 表示行に描画成功したチップが0件の場合は、点線枠の「チップを配置できます」を表示する。0行設定の非systemベイは従来どおり非表示とした。
- 空行案内は`aria-hidden`かつ`pointer-events: none`とし、未知チップの保存状態、ツールボックス追加、配置済みチップ移動のD&D契約を変更していない。
- UI-4対象テスト、全183テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- UI-5の実行GOは未取得であり、次はUI-5「全自動回帰とFirefox表示確認」から再開する。
- UI改善 UI-5「全自動回帰とFirefox表示確認」を完了し、状態を`UI-5_COMPLETE`とした。
- Firefox実機で統合ツールバー、タイトル情報チップ、system固定枠、3面の視覚階層、空ベイ表示を確認し、ユーザーから全項目の正常動作確認を得た。
- UI-5Aとして、開発ビルドのDB-15性能計測処理と保存契約を維持したまま、左下の計測パネルを常時非表示にした。
- UI-5A対象テスト、全183テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- パネル背景色フレーバー BG-1〜BG-3のRed-Greenを完了し、状態を`BG-3_COMPLETE`とした。
- ページ表示ごとのseedとブックマークGUIDから8種類のフレーバーを選び、同じ画面内では検索、ソート、表示切替後も同じブックマークの色を維持するようにした。
- フレーバーはスケーリングするパネル表示だけへ適用し、一覧、アイコン、カード表示には適用しない境界を追加した。
- ライト／ダークで対になる背景、通常文字、補助文字、枠線を定義し、全16背景で文字コントラスト比4.5:1以上、枠線3:1以上を自動検証した。
- BG-1〜BG-3対象6テスト、全186テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- パネル背景色フレーバー BG-4「Firefox実機確認」を完了し、状態を`BG-4_COMPLETE`とした。
- Firefox実機でパネルモード限定の背景色、同一画面内の色維持、他3表示への非適用、ライト／ダークの可読性、全サイズとD&D表示を確認し、ユーザーから全項目の正常動作確認を得た。
- パネル配色設定 PF-1「16色パレットと永続設定モデル」のRed-Greenを完了し、状態を`PF-1_COMPLETE`とした。
- パレットを16色へ拡張し、全ライト／ダーク配色で文字コントラスト比4.5:1以上、枠線3:1以上を維持した。
- v1文書として32bit seedとGUID別overrideだけを専用`storage.local`キーへ防御的に保存するモデルを追加した。
- 未保存・破損値の正規化、個別指定優先、自動配色への復帰、不正UI入力の拒否、Firefox全ブックマークGUIDによる孤児override除去を追加した。
- PF-1対象3テスト、全187テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- パネル配色設定 PF-2「初期読み込みへの保存配色統合」のRed-Greenを完了し、状態を`PF-2_COMPLETE`とした。
- 保存済み配色をFirefox全ブックマークツリーで正常化・孤児除去し、補正保存が成功するまで通常runtimeへ公開しないようにした。
- 初回パネル描画から永続seedとGUID別overrideを使用し、検索、ソート、表示切替後も同じ保存設定を参照するようにした。
- PF-2対象3テスト、全187テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- パネル配色設定 PF-3「パネル歯車と配色ポップオーバー」のRed-Greenを完了し、状態を`PF-3_COMPLETE`とした。
- パネルモードの各ブックマークへhover／キーボードフォーカス時に表示する配色歯車を追加し、最小2サイズではパネル全面へ切り替えて操作可能にした。
- 単一の配色ピッカーへ「自動」と16色を描画し、現在の個別指定を選択状態で示すようにした。
- 配色選択の正常保存後だけGUID別overrideを正式状態へ昇格し、パネルを即時再描画するようにした。「自動」はoverrideを削除してseed配色へ戻す。
- PF-3対象4テスト、既存CSS契約を含む全189テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- PF-4以降の実装GOは未取得であり、次はPF-4「保存失敗復旧とクリック・D&D分離」のテストケース確認とRed-Green GO確認から再開する。
- 一覧表改善 LIST-1「表示モデルと日付設定モデル」のRed-Greenを完了し、状態を`LIST-1_COMPLETE`とした。
- 一覧モデルへ登録日時、最終訪問日時、訪問回数の表示値を追加し、欠損・不正値を`—`、取得済み未訪問を`0`として区別した。現行DOM互換のドメイン値はLIST-2まで暫定保持する。
- 一覧日時設定を専用v1文書として定義し、初期値をブラウザー方式、対応方式をブラウザー、ISO、日本、米国、英国、欧州として正常化するモデルと`storage.local`読み書きを追加した。
- 地域方式は`Intl.DateTimeFormat`へ委譲し、ISO方式もブラウザーと同じローカルタイムゾーンで秒を省略する。対象2テスト、全190テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- LIST-2以降の実装GOは未取得であり、次はLIST-2「高密度テーブル描画」のRed-Green GO確認から再開する。
- 一覧表改善 LIST-2「高密度テーブル描画」のRed-Greenを完了し、状態を`LIST-2_COMPLETE`とした。
- 一覧DOMを5列の意味的tableへ置き換え、アイコン見出しを可視表示せず、タイトル、登録日時、最終訪問日時、訪問回数の見出しと値を描画した。
- 行高24px、favicon 16px、全セル左揃え、通常行の透明背景、hover行だけのグレー背景へ変更し、一覧全体の背景と外枠を撤去した。
- テーブル最小幅760pxと横スクロール容器を追加し、狭幅でも列を非表示にしない契約へ変更した。現行の行クリック、data属性、キーボードフォーカス、D&D境界契約は維持している。
- LIST-2対象2テスト、全190テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- LIST-3以降の実装GOは未取得であり、次はLIST-3「列ヘッダーソート」のRed-Green GO確認から再開する。
- 一覧表改善 LIST-3「列ヘッダーソート」のRed-Greenを完了し、状態を`LIST-3_COMPLETE`とした。
- タイトル、登録日時、最終訪問日時、訪問回数の列見出しをキーボード操作可能なbuttonへ変更し、別軸の初回選択を昇順、同一軸の再選択を方向反転とした。
- 現在の列と方向を`▲`／`▼`および`aria-sort`で表示し、一覧ヘッダーからの操作を既存ソートチップと同じ共有状態更新経路へ接続した。
- LIST-3対象3テスト、全190テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- LIST-4以降の実装GOは未取得であり、次はLIST-4「日付設定オーバーレイ」のRed-Green GO確認から再開する。
- 一覧表改善 LIST-4「日付設定オーバーレイ」のRed-Greenを完了し、状態を`LIST-4_COMPLETE`とした。
- 一覧右上へアクセシブルな歯車を追加し、ブラウザー、ISO、日本、米国、英国、欧州の6方式を選択できる固定オーバーレイへ接続した。
- 選択変更を専用`storage.local`設定へ即時保存し、成功後だけ正式状態へ昇格して一覧日時を再描画する。設定は起動時に読み込み、未保存・破損値はブラウザー方式へ補正保存する。
- オーバーレイは外側操作では閉じず、`×`操作まで維持する。保存失敗時は直前値へ復元し、オーバーレイ内へ失敗状態を表示する。
- LIST-4対象5テスト、全192テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- LIST-5以降の実装GOは未取得であり、次はLIST-5「モード制約と総合回帰」のRed-Green GO確認から再開する。
- 一覧列幅変更 LIST-5A「列幅モデルと永続化契約」のRed-Greenを完了し、状態を`LIST-5A_COMPLETE`とした。
- アイコン、タイトル、登録日時、最終訪問日時、訪問回数の初期幅を24px、292px、160px、160px、84px、最小幅を24px、60px、64px、64px、36pxとして定義した。
- 各列を独立して更新し最小幅へ補正する不変モデルと、専用`storage.local`キー`listColumnWidthPreferences.v1`の読み書きを追加した。未保存・破損・余剰項目を含む保存値は既定値または最小幅へ正常化する。
- LIST-5A対象4テスト、全193テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- LIST-5Bの実装GOは未取得であり、次はLIST-5B「5本の列境界ハンドルと一覧UI統合」のRed-Green GO確認から再開する。
- 一覧列幅変更 LIST-5B「5本の列境界ハンドルと一覧UI統合」のRed-Greenを完了し、状態を`LIST-5B_GREEN`とした。
- 全列の見出し右端へ`|`形の境界線と9pxの水平リサイズ領域を追加した。右端を含む各ハンドルは左側の対象1列だけを変更し、表全幅の増減を既存の横スクロールへ反映する。
- ドラッグ中は列と表全幅へ即時反映し、完了時だけ対象列を`listColumnWidthPreferences.v1`へ保存する。キャンセル時は開始幅へ戻し、保存失敗時は正式状態を更新せず保存済み幅へ再描画する。
- 起動時に保存済み列幅を読み込み、正常化・補正保存が成功してから通常runtimeへ公開する接続を追加した。
- LIST-5B対象8テスト、全193テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 自動回帰は成功済みであり、次はFirefox実機で列境界、最小幅、横スクロール、再起動後の永続化を確認する。
- Firefox実機でタイトル列右端のドラッグがタイトル列だけの幅を変更し、右隣の登録日時列幅を維持することを確認し、ユーザーからPASSを得た。
- 一覧列幅変更 LIST-5C「列幅初期化ボタン」のRed-Greenを完了し、状態を`LIST-5C_GREEN`とした。
- アイコン列見出しへ`↺`ボタンを追加し、確認なしで5列を初期幅へ即時再描画して専用列幅設定へ保存するようにした。保存失敗時は直前幅へ復元し、日付表示方式の状態と保存経路は変更していない。
- LIST-5C対象2テスト、全193テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次はFirefox実機で列幅初期化と日付表示方式の非変更を確認する。
- 表示モードグリフ VMG-1「同幅5ボタンとお気に入り選択モック」のRed-Greenを完了し、状態を`VMG-1_GREEN`とした。
- 表示形式チップを文字主体の4択から、★、四隅を丸く抜いたパネル、既存ブックマーク画像、右上切れ込みカード、横線4本一覧の同幅5択へ変更した。
- ★は排他的な選択表示だけをruntime内で同期し、正式な表示モード状態、保存値、メイン表示内容を変更しない。既存4モードを選ぶと★選択を解除して従来の表示切替を行う。
- 全194テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次はFirefox実機で5グリフの形状、同幅、選択表示、★選択時の表示維持を確認する。
- Firefox初回確認はPASS後に見た目修正を実施した。パネルと一覧を10%縮小し、既存ブックマークSVGを`currentColor`マスクへ変更し、カード右上を斜辺で閉じ、★の斜体を解除した。ユーザー指定によりTDDは省略し、修正後の全194テスト、拡張lint、`git diff --check`を再確認した。
- 修正後のFirefox確認で、ボタン本体の子孫`span`セレクターが内側グリフにも寸法と境界線を二重適用する崩れを確認した。直下`span`だけへ限定してグリフ内部へのCSS漏れを修正した。
- フォルダ欄・アイテム欄の上下分離 DIR-1「仕様書と実装前テストケース確定」を完了し、状態を`DIR-1_COMPLETE`とした。
- 上側フォルダ欄と下側URLアイテム欄を個別の背景・外枠・内部スクロールで分離し、枠間の2つの文字ボタンでシーン内希望段数を1段階ずつ変更する仕様を`docs/folder_item_frames.md`へ記録した。
- 永続化する既定段数を初期3、範囲1～5とし、フォルダ必要段数と画面高25%上限から実効段数を自動縮小する。フォルダ移動、表示モード切替、再読み込み等を一時調整の破棄境界として確定した。
- 正常、空、境界、シーン遷移、保存失敗、狭小画面を含むDIR-TC-01～20と、DIR-2～DIR-7のTDD実装フェーズおよびコストを確定した。
- DIR-2以降の実装GOは未取得であり、次はDIR-2「段数状態と実効段数モデル」のRed–Green GO確認から再開する。
- フォルダ欄・アイテム欄の上下分離 DIR-2「段数状態と実効段数モデル」のRed–Greenを完了し、状態を`DIR-2_COMPLETE`とした。
- 初期3段、範囲1～5の既定段数とシーン内希望段数を分離し、フォルダ必要段数および画面高25%相当の上限から最低1段を守る実効段数を求める純粋モデルを追加した。
- フォルダ欄・アイテム欄の1段階拡大、上下限、シーン内希望の破棄、既定段数変更時の現シーン適用を入力非破壊で実装した。自動縮小や画面上限補正後も希望段数を保持する。
- Redは新規モデル未実装による`ERR_MODULE_NOT_FOUND`で成立し、DIR-2対象11テスト、全195テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- モデルは十分に小さく責務が限定されているため、独立したRefactorフェーズは不要と判断した。
- DIR-3以降の実装GOは未取得であり、次はDIR-3「既定段数の保存と設定モーダル」のRed–Green GO確認から再開する。
- フォルダ欄・アイテム欄の上下分離 DIR-3「既定段数の保存と設定モーダル」のRed–Greenを完了し、状態を`DIR-3_COMPLETE`とした。
- 専用キー`folderFrameRowPreferences.v1`へversion 1と既定段数だけを保存し、未保存・破損・範囲外・余剰項目を初期3段へ補正する永続化境界を追加した。補正保存成功後だけ通常runtimeへ公開する。
- フォルダ欄右上の歯車から、1～5の整数だけを受け付けるスピンボックス設定モーダルを開く導線を追加した。`×`は保存成功後に閉じて現在シーンへ適用し、Escapeは破棄、外側操作は維持、保存失敗は正式値へ戻して固定エラーを表示する。
- Redは新規保存モデル・コントローラー未実装の`ERR_MODULE_NOT_FOUND`と設定DOM未実装で成立し、DIR-3対象3テスト群、全198テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 保存モデルとモーダル制御は既存責務から分離済みで重複も小さいため、独立したRefactorフェーズは不要と判断した。
- DIR-4以降の実装GOは未取得であり、次はDIR-4「上下枠、独立スクロール、枠間操作」のRed–Green GO確認から再開する。
- フォルダ欄・アイテム欄の上下分離 DIR-4「上下枠、独立スクロール、枠間操作」のRed–Greenを完了し、状態を`DIR-4_COMPLETE`とした。
- 上側フォルダ欄と下側URLアイテム欄を個別の背景・単一外枠・最低48px・内部縦スクロールを持つflex領域へ分離し、0件でもフォルダ欄を1段分表示するよう変更した。
- 枠間へ指定文言の2ボタンを配置し、シーン内希望段数を1段ずつ増減する純粋モデルへ接続した。段階操作は内容DOMを再構築せず高さとボタン可否だけを更新するため、両欄のスクロール位置を維持する。
- 固定144pxフォルダの必要段数と中央領域高25%以内の上限を計測し、実効段数を36px行・6px間隔・外枠余白へ変換する表示境界を追加した。上下限では対応する操作だけを無効化する。
- Redは新規枠配分ビュー未実装による`ERR_MODULE_NOT_FOUND`で成立し、対象4テスト、全199テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- Green中に上下枠の共有外観CSSを統合し、既存の中央領域スクロールとレイアウトゴミ箱DOM検査を新しい入れ子構造へ追従させたため、追加Refactorは不要と判断した。
- DIR-5以降の実装GOは未取得であり、次はDIR-5「シーン境界のruntime接続」のRed–Green GO確認から再開する。
- フォルダ欄・アイテム欄の上下分離 DIR-5「シーン境界のruntime接続」のRed–Greenを完了し、状態を`DIR-5_COMPLETE`とした。
- フォルダ移動、履歴の戻る／進む、4表示モード切替、レイアウト再構築でシーン内希望段数を破棄し、既定段数へ戻すruntime境界を追加した。
- フォルダ移動と履歴移動では両欄、表示モード切替ではアイテム欄だけを先頭へ戻す。検索、フィルター、ソート、リサイズは一時調整を維持する。
- DIR-6以降の実装GOは未取得であり、次はDIR-6「狭小画面と総合回帰」のRed–Green GO確認から再開する。
- フォルダ欄・アイテム欄の上下分離 DIR-6「狭小画面と総合回帰」のRed–Greenを完了し、状態を`DIR-6_COMPLETE`とした。
- 狭幅時に枠間の2文字ボタンを折り返し、両欄へ`scrollbar-gutter: stable`を適用して縦スクロールバー出現時の内容幅変動を抑えた。
- 最低48pxの上下枠、中央領域の外側スクロール、0件時の自然な空欄、URL4表示モードのアイテム欄内限定、新規作成導線が未実装のままであることを境界テストへ固定した。
- Redはボタン折り返しと安定スクロールバー未実装で成立し、対象6テスト群、全201テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- DIR-7の実機確認GOは未取得であり、次はFirefoxで枠、段階操作、スクロール、設定、シーン境界、再起動復元を確認する。
- Firefox実機確認で、フォルダ0件時に見た目が変わらないまま内部希望段数だけが進み、後からボタンがグレー化する問題を確認した。
- DIR-7F-2で両方向の操作後の実効段数を事前計算し、片方向でも表示が変わらない場合は両ボタンを同時に無効化して内部状態を変更しない契約へ修正した。フォルダ数・画面寸法変更時は可否を再計算する。
- 実公開前導入 EXP-ROOT-1「experiment専用ブックマークroot確保」のRed–Greenを完了し、状態を`EXP-ROOT-1_COMPLETE`とした。
- Firefoxルート直下に`bookumark`フォルダがなければ1回だけ作成し、既にあれば先頭の同名フォルダを内容非変更で再利用する純粋な非同期境界を追加した。同名URLとseparatorはフォルダとして扱わない。
- 子要素取得またはフォルダ作成に失敗した場合は、原因を結果へ保持してFirefoxルートへフォールバックする。画面通知とexperiment専用起動接続はEXP-ROOT-2へ分離した。
- Redは既存production build成功後、新規モジュール未実装による`ERR_MODULE_NOT_FOUND`で成立した。対象5テスト、全202テストの成功を確認した。
- 独立責務の小さな関数であり追加Refactorは不要と判断した。次はEXP-ROOT-2「experiment起動時だけ初期rootへ接続」のRed–Green GO確認から再開する。
- 実公開前導入 EXP-ROOT-2「experiment起動時だけ初期rootへ接続」のRed–Green自動化部分を完了し、状態を`EXP-ROOT-2_GREEN`とした。
- development buildのパネルHTMLだけへ`data-experiment-mode="true"`を付与し、production buildには付与しないビルド境界を追加した。
- production起動は保存済みフォルダ復元を維持し、experiment起動は保存位置より`root/bookumark`を優先する。新規作成後だけツリーを再取得し、既存フォルダ再利用時は余分な再取得を行わない。
- experimentフォルダの検索・作成・作成後再取得に失敗した場合は、実rootで起動を継続し、内部原因を本文へ出さない固定errorトーストを共通通知へ追加した。成功時は通知しない。
- Redは初期root調停モジュールとdevelopmentマーカー未実装で成立した。対象4テスト群、全204テスト、production/developmentビルド境界、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 次はFirefox実機でexperiment初回起動時の`bookumark`自動作成・初期表示と、`<`による実root移動を確認する。
- EXP-ROOT-2のFirefox実機確認では、仮想root直下への`bookumark`作成がFirefox APIの`The bookmark root cannot be modified`制約により失敗し、実rootが初期表示された。
- ユーザー判断により保存先を別のFirefox標準フォルダへ変更せず、作成失敗時にエラー通知を出して実rootへフォールバックする確定済み仕様を適用する。仮想root直下に作成できる環境では従来どおり`bookumark`を初期表示する。
- Firefox実機で作成失敗時の固定errorトースト表示についてユーザーPASSを得た。実rootへのフォールバックと起動継続を含め、EXP-ROOT-2を`EXP-ROOT-2_COMPLETE`とした。
- 次はAMO-1「production ZIP・審査用ソースZIP生成コマンド」のテストケース確認とRed–Green GO確認から再開する。
- 実公開前導入 AMO-1「production ZIP・審査用ソースZIP生成コマンド」のRed–Greenを完了し、状態を`AMO-1_COMPLETE`とした。
- `npm run build:amo`でバージョン一致を検証し、`amo-artifacts/`を作り直してproduction拡張ZIPとAMO審査用ソースZIPを同時生成するようにした。AMO送信・署名処理は含まない。
- ソースZIPは許可リスト方式とし、再ビルドに必要なmanifest、package文書、lockfile、TypeScript設定、productionビルドスクリプト、TypeScriptソース、HTML、CSS、SVG、ライセンス、説明、AMOビルド手順だけを格納する。
- 実生成監査で`panel/`配下の無視済み旧JS・source map混入を検出し、HTML・CSS・SVGの個別指定へ狭めて再生成した。`dist/`、`node_modules/`、`.git/`、`.env`、成果物、テスト、個人用文書はソースZIPへ含めない。
- 両ZIPの展開整合性と内容を確認した。Redは成果物計画モジュール未実装による`ERR_MODULE_NOT_FOUND`で成立し、対象5テスト、全205テスト、production build、Firefox拡張lint（errors 0／warnings 0／notices 0）、`git diff --check`の成功を確認した。
- 現在生成した`0.1.0`のZIPはパイプライン検証用で提出候補ではない。次はAMO-2「0.1.1同期と提出・導入手順」のRed–Green GO確認から再開する。
